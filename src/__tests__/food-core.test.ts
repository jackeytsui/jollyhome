describe('food core scaffold', () => {
  it('keeps the Phase 4 core test lane active', () => {
    expect(true).toBe(true);
  });

  it.todo('D-Phase4-canonical-catalog groups shopping, pantry, recipes, and receipt rows around canonical catalog items');
  it.todo('D-Phase4-threshold-restock preserves idempotent restock generation when multiple low-stock signals arrive together');
});
