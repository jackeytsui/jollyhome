describe('shopping and supplies UI scaffold', () => {
  it('keeps the Phase 4 shopping UI test lane active', () => {
    expect(true).toBe(true);
  });

  it.todo('D-Phase4-canonical-catalog presents grouped catalog suggestions consistently across shopping and pantry flows');
  it.todo('D-Phase4-pantry-photo manual review prevents direct inventory writes before a human accepts suggestions per AISH-03');
});
