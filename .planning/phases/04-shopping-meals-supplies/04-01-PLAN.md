---
phase: 04-shopping-meals-supplies
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/types/shopping.ts
  - src/types/inventory.ts
  - src/types/recipes.ts
  - src/types/meals.ts
  - src/__tests__/food-core.test.ts
  - src/__tests__/food-hooks.test.ts
  - src/__tests__/shopping-supplies-ui.test.ts
  - src/__tests__/meals-ui.test.ts
  - src/__tests__/food-ai.test.ts
  - src/__tests__/receipt-food-flow.test.ts
autonomous: true
requirements:
  - SHOP-05
  - SHOP-06
  - SHOP-09
  - MEAL-01
  - MEAL-04
  - MEAL-06
  - AIML-03
  - AIML-04
  - AISH-01
  - AIEX-04
  - SYNC-01
  - SYNC-03
  - SYNC-04
  - SYNC-05
  - SYNC-07
must_haves:
  truths:
    - "Phase 4 starts with one explicit food-domain contract spanning shopping lists, pantry inventory, recipes, meal plans, AI suggestions, and receipt-sync outputs."
    - "Executors can build food screens and server workflows against passing scaffold tests instead of guessing future behavior."
    - "All new Phase 4 dependencies are installed before the feature plans rely on barcode scanning, recipe import parsing, or drag-and-drop meal authoring."
  artifacts:
    - path: "src/types/inventory.ts"
      provides: "Canonical food catalog, inventory snapshot, movement ledger, and low-stock alert contracts."
      contains: "export interface InventoryEvent"
    - path: "src/types/meals.ts"
      provides: "Meal-plan entry, AI suggestion run, and feedback contracts."
      contains: "export interface MealPlanEntry"
    - path: "src/__tests__/receipt-food-flow.test.ts"
      provides: "Scaffold coverage for the one-photo-three-workflows commit path."
      contains: "describe('grocery receipt workflow'"
  key_links:
    - from: "package.json"
      to: "src/__tests__/meals-ui.test.ts"
      via: "meal-board dependency installation for drag-and-drop authoring"
      pattern: "react-native-draggable-flatlist|expo-camera|cheerio"
    - from: "src/types/shopping.ts"
      to: "src/types/inventory.ts"
      via: "shopping rows refer to canonical food items and threshold-driven restock data"
      pattern: "catalogItemId|minQuantity"
    - from: "src/types/meals.ts"
      to: "src/types/recipes.ts"
      via: "meal slots store recipe references and AI provenance"
      pattern: "recipeId|suggestionRunId"
---

<objective>
Create the narrow Phase 4 entry point: install the approved food-domain dependencies, define stable contracts, and add passing scaffold tests for every execution lane before database or UI work begins.

Purpose: This keeps the largest phase from starting with ambiguous interfaces or missing validation coverage.
Output: Installed dependencies, Phase 4 type files, and passing scaffold test files for food core, hooks, UI, AI, and receipt-sync flows.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-shopping-meals-supplies/04-RESEARCH.md
@.planning/phases/04-shopping-meals-supplies/04-VALIDATION.md
@src/types/calendar.ts
@src/hooks/useReceipt.ts
@src/hooks/useProfile.ts

<interfaces>
From src/types/calendar.ts:
```typescript
export type CalendarActivityType = 'event' | 'meal' | 'maintenance' | 'guest' | 'quiet_hours' | 'booking';

export interface HouseholdCalendarItem {
  sourceType: 'event' | 'chore' | 'attendance' | 'meal' | 'maintenance' | 'guest' | 'quiet-hours' | 'booking';
  startsAt: string;
  endsAt: string;
  metadata: Record<string, unknown> | null;
}
```

From src/hooks/useReceipt.ts:
```typescript
export interface ReceiptData {
  store_name: string;
  date: string | null;
  items: ReceiptItem[];
  subtotal_cents: number;
  tax_cents: number;
  tip_cents: number;
  total_cents: number;
}
```

From src/hooks/useProfile.ts:
```typescript
export interface Profile {
  dietary_preferences: string[];
  dietary_notes: string | null;
  active_household_id: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the Phase 4 food dependencies and write passing scaffold suites</name>
  <files>package.json, package-lock.json, src/__tests__/food-core.test.ts, src/__tests__/food-hooks.test.ts, src/__tests__/shopping-supplies-ui.test.ts, src/__tests__/meals-ui.test.ts, src/__tests__/food-ai.test.ts, src/__tests__/receipt-food-flow.test.ts</files>
  <action>Install the exact Phase 4 research-backed packages `expo-camera`, `react-native-draggable-flatlist`, and `cheerio` per the 2026-03-23 research snapshot. Create the six test files as passing scaffold suites only. Reserve explicit placeholders for D-Phase4-canonical-catalog grouping, D-Phase4-threshold-restock idempotency, D-Phase4-attendance-based servings per SYNC-07, D-Phase4-AI swap/regenerate feedback persistence per AIML-04, D-Phase4-pantry-photo manual review per AISH-03, and D-Phase4-atomic grocery receipt commit per AIEX-04 / SYNC-01. Do not introduce failing assertions in this plan.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/food-core.test.ts src/__tests__/food-hooks.test.ts src/__tests__/shopping-supplies-ui.test.ts src/__tests__/meals-ui.test.ts src/__tests__/food-ai.test.ts src/__tests__/receipt-food-flow.test.ts</automated>
  </verify>
  <done>The new dependencies are declared and all six Phase 4 test files run as scaffold suites with no failures.</done>
</task>

<task type="auto">
  <name>Task 2: Define the Phase 4 food contracts</name>
  <files>src/types/shopping.ts, src/types/inventory.ts, src/types/recipes.ts, src/types/meals.ts</files>
  <action>Create `src/types/shopping.ts` with exported contracts for `ShoppingList`, `ShoppingListItem`, `ShoppingCategoryKey`, and generated-restock metadata so SHOP-01 through SHOP-05 have one consistent item shape. Create `src/types/inventory.ts` with `FoodCatalogItem`, `InventoryItem`, `InventoryEvent`, and `InventoryAlert`, using a canonical `catalogItemId` link and immutable event source types per SHOP-06, SHOP-07, AISH-01, and SYNC-05. Create `src/types/recipes.ts` with `Recipe`, `RecipeIngredient`, `RecipeImportDraft`, and recipe tag/favorite fields per MEAL-02, MEAL-03, and MEAL-07. Create `src/types/meals.ts` with `MealPlanEntry`, `MealSuggestionRun`, `MealSuggestion`, and `MealSuggestionFeedback`, including attendance-derived servings, accept/swap/regenerate state, and suggestion feedback fields per MEAL-06 and AIML-03 / AIML-04.</action>
  <verify>
    <automated>rg -n "export interface (ShoppingList|ShoppingListItem|FoodCatalogItem|InventoryEvent|Recipe|RecipeIngredient|MealPlanEntry|MealSuggestionFeedback)|catalogItemId|attendanceMemberIds|suggestionRunId|minimumQuantity" src/types/shopping.ts src/types/inventory.ts src/types/recipes.ts src/types/meals.ts</automated>
  </verify>
  <done>The Phase 4 domain contracts exist and downstream plans can use exact field names for shopping, inventory, recipes, meals, AI feedback, and receipt-sync payloads.</done>
</task>

</tasks>

<verification>
Run all Phase 4 scaffold test files together and confirm the four type files export the exact food-domain contracts later plans reference.
</verification>

<success_criteria>
Phase 4 begins with explicit food-domain contracts, installed libraries, and passing scaffold tests instead of ad hoc schema and UI assumptions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-shopping-meals-supplies/04-01-SUMMARY.md`
</output>
