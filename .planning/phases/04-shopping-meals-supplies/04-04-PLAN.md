---
phase: 04-shopping-meals-supplies
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - src/app/(app)/shopping.tsx
  - src/app/(app)/supplies.tsx
  - src/components/shopping/ShoppingListTabs.tsx
  - src/components/shopping/ShoppingItemRow.tsx
  - src/components/shopping/ShoppingItemEditorSheet.tsx
  - src/components/shopping/AisleGroupSection.tsx
  - src/components/supplies/InventoryCard.tsx
  - src/components/supplies/ThresholdEditorSheet.tsx
  - src/components/supplies/BarcodeScannerSheet.tsx
  - src/components/supplies/LowStockAlertCard.tsx
  - src/__tests__/shopping-supplies-ui.test.ts
autonomous: true
requirements:
  - SHOP-01
  - SHOP-02
  - SHOP-03
  - SHOP-04
  - SHOP-05
  - SHOP-06
  - SHOP-07
  - SHOP-08
  - SYNC-05
must_haves:
  truths:
    - "Users can manage multiple real-time household shopping lists, add/edit items, and keep checked rows visible at the bottom instead of deleting them."
    - "Shopping rows group by aisle/category using the canonical food taxonomy rather than free-text sorting."
    - "Users can view pantry stock, adjust thresholds, scan barcodes, and see low-stock items surface back into the active list."
  artifacts:
    - path: "src/app/(app)/shopping.tsx"
      provides: "Shared shopping list screen with multi-list selection and grouped item rendering."
      contains: "ShoppingListTabs"
    - path: "src/app/(app)/supplies.tsx"
      provides: "Pantry inventory screen with threshold and barcode workflows."
      contains: "BarcodeScannerSheet"
    - path: "src/components/supplies/LowStockAlertCard.tsx"
      provides: "Visible low-stock notice surface tied to active-list restock behavior."
      contains: "auto-added"
  key_links:
    - from: "src/app/(app)/shopping.tsx"
      to: "src/hooks/useShopping.ts"
      via: "screen state and item mutations flow through the base shopping hook"
      pattern: "useShopping"
    - from: "src/app/(app)/supplies.tsx"
      to: "src/hooks/useInventory.ts"
      via: "inventory stock and threshold writes drive low-stock behavior"
      pattern: "useInventory"
    - from: "src/components/supplies/BarcodeScannerSheet.tsx"
      to: "expo-camera"
      via: "barcode scan result hydrates inventory or list-item add flows"
      pattern: "CameraView|barcodeScannerSettings"
---

<objective>
Ship the shopping and pantry surfaces: real-time list management, aisle grouping, pantry inventory, threshold editing, barcode scanning, and visible low-stock restock behavior.

Purpose: This is the first user-facing Wave 4 slice and it validates the food-domain hooks with the most immediate household workflow.
Output: `shopping.tsx`, `supplies.tsx`, shopping components, supplies components, and green shopping/supplies UI tests.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-shopping-meals-supplies/04-RESEARCH.md
@.planning/phases/04-shopping-meals-supplies/04-03-PLAN.md
@src/app/(app)/finances.tsx
@src/components/receipt/ReceiptCameraView.tsx
@src/components/expenses/QuickAddCard.tsx

<interfaces>
From src/hooks/useShopping.ts:
```typescript
export function useShopping();
// Must expose active list selection, grouped items, create/update item, and checked toggle helpers.
```

From src/hooks/useInventory.ts:
```typescript
export function useInventory();
// Must expose inventory items, low-stock alerts, stock adjustments, threshold updates, and barcode resolution helpers.
```

From src/components/receipt/ReceiptCameraView.tsx:
```typescript
export function ReceiptCameraView(props: { visible: boolean; onCapture(uri: string): void; onClose(): void; });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the shared shopping-list surface and grouped item flows</name>
  <files>src/app/(app)/shopping.tsx, src/components/shopping/ShoppingListTabs.tsx, src/components/shopping/ShoppingItemRow.tsx, src/components/shopping/ShoppingItemEditorSheet.tsx, src/components/shopping/AisleGroupSection.tsx, src/__tests__/shopping-supplies-ui.test.ts</files>
  <behavior>- Users can switch between multiple household lists, add/edit items with quantity/category/notes, and see item rows grouped by aisle/category.\n- Checked items remain visible and sorted to the bottom rather than disappearing.\n- Screen updates reflect the shared food realtime hook instead of manual refresh gestures.</behavior>
  <action>Turn the relevant sections of `src/__tests__/shopping-supplies-ui.test.ts` into real assertions first, then build the shopping screen and components. Follow the repo’s card/sheet pattern: use focused bottom-sheet or modal editors rather than route-heavy CRUD. Implement multi-list tabs per SHOP-01, item editor fields for SHOP-02, checked-item bottom ordering for SHOP-04, and aisle/category sections driven by canonical category keys for SHOP-05. Use the `useShopping` hook exclusively for loading and mutations; do not issue direct Supabase calls from the screen.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/shopping-supplies-ui.test.ts</automated>
  </verify>
  <done>The shopping surface supports multiple real-time lists, grouped items, and persistent checked rows without screen-local backend logic.</done>
</task>

<task type="auto">
  <name>Task 2: Build pantry inventory, threshold, barcode, and low-stock surfaces</name>
  <files>src/app/(app)/supplies.tsx, src/components/supplies/InventoryCard.tsx, src/components/supplies/ThresholdEditorSheet.tsx, src/components/supplies/BarcodeScannerSheet.tsx, src/components/supplies/LowStockAlertCard.tsx, src/__tests__/shopping-supplies-ui.test.ts</files>
  <action>Create the pantry inventory screen and supporting components using `useInventory`. Include current stock display, quick manual adjustments, minimum-threshold editing per SHOP-07, and a barcode scanner sheet using `expo-camera` barcode support for SHOP-08. Surface low-stock alerts visibly on the supplies screen and ensure restock-triggered items point back to the active shopping list per SYNC-05. Keep barcode metadata lookup best-effort only; manual correction stays available if lookup data is missing or wrong, per the research anti-pattern guidance.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/shopping-supplies-ui.test.ts</automated>
  </verify>
  <done>The pantry surface lets users manage stock, thresholds, and barcode adds while visibly reflecting low-stock auto-restock behavior.</done>
</task>

</tasks>

<verification>
Run the shopping/supplies UI test suite and confirm the two food screens use the shared hooks, not raw backend calls.
</verification>

<success_criteria>
Users can shop collaboratively and manage household supplies with the polish and stock-awareness expected from Phase 4.
</success_criteria>

<output>
After completion, create `.planning/phases/04-shopping-meals-supplies/04-04-SUMMARY.md`
</output>
