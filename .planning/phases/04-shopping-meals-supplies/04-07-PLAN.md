---
phase: 04-shopping-meals-supplies
plan: "07"
type: execute
wave: 5
depends_on:
  - "04-03"
  - "04-04"
files_modified:
  - supabase/migrations/00006_food_receipt_sync.sql
  - supabase/functions/commit-grocery-receipt/index.ts
  - src/lib/receiptWorkflow.ts
  - src/hooks/useReceipt.ts
  - src/components/receipt/ReceiptReviewCard.tsx
  - src/app/(app)/finances.tsx
  - src/__tests__/receipt-food-flow.test.ts
autonomous: true
requirements:
  - AIEX-04
  - SYNC-01
must_haves:
  truths:
    - "One reviewed grocery receipt confirmation can create an expense split, update pantry inventory, and check off matching shopping rows without partial state."
    - "The existing OCR/review step stays intact while final persistence moves behind one server-side workflow."
    - "Receipt items map into canonical food items and shopping-list matches before commit so cross-feature sync is deterministic."
  artifacts:
    - path: "supabase/functions/commit-grocery-receipt/index.ts"
      provides: "Atomic grocery receipt commit workflow."
      contains: "shoppingListMatchIds"
    - path: "src/lib/receiptWorkflow.ts"
      provides: "Client wrapper for reviewed grocery receipt commit payload assembly."
      contains: "commitGroceryReceipt"
    - path: "src/__tests__/receipt-food-flow.test.ts"
      provides: "Coverage for the one-photo-three-workflows save path."
      contains: "creates expense"
  key_links:
    - from: "src/app/(app)/finances.tsx"
      to: "src/lib/receiptWorkflow.ts"
      via: "reviewed grocery receipt confirmation routes through the atomic workflow wrapper"
      pattern: "commitGroceryReceipt"
    - from: "src/hooks/useReceipt.ts"
      to: "supabase/functions/commit-grocery-receipt/index.ts"
      via: "reviewed OCR payload is extended with catalog and shopping matches before save"
      pattern: "commit-grocery-receipt"
    - from: "supabase/functions/commit-grocery-receipt/index.ts"
      to: "supabase/migrations/00006_food_receipt_sync.sql"
      via: "receipt commit uses the dedicated SQL function / schema additions for transactional writes"
      pattern: "grocery_receipt_commits|perform_grocery_receipt_commit"
---

<objective>
Deliver the Phase 4 magic moment: after OCR review, one grocery receipt confirmation atomically writes the expense, pantry updates, and shopping-list checkoffs.

Purpose: This is the highest-risk cross-feature workflow in the phase and it must be isolated behind one server-side commit boundary.
Output: `00006_food_receipt_sync.sql`, `commit-grocery-receipt` Edge Function, receipt workflow client wrapper, finances receipt-review integration, and green receipt-sync tests.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-shopping-meals-supplies/04-RESEARCH.md
@.planning/phases/02-expense-tracking-receipt-ocr/02-06-SUMMARY.md
@src/hooks/useReceipt.ts
@src/components/receipt/ReceiptReviewCard.tsx
@src/types/expenses.ts

<interfaces>
From src/hooks/useReceipt.ts:
```typescript
export interface ReceiptItem {
  name: string;
  price_cents: number;
  classification: 'shared' | 'personal';
  suggested_owner: string | null;
}

export interface ReceiptData {
  store_name: string;
  items: ReceiptItem[];
  tax_cents: number;
  tip_cents: number;
  total_cents: number;
}
```

From src/types/expenses.ts:
```typescript
export interface CreateExpenseInput {
  household_id: string;
  payer_user_id: string;
  amount_cents: number;
  title: string;
  category: string;
  split_type: SplitType;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the transactional grocery receipt commit backend</name>
  <files>supabase/migrations/00006_food_receipt_sync.sql, supabase/functions/commit-grocery-receipt/index.ts, src/lib/receiptWorkflow.ts, src/__tests__/receipt-food-flow.test.ts</files>
  <behavior>- A reviewed grocery receipt commit either writes all three outputs or none: expense, pantry inventory events, and shopping-list item checkoffs.\n- Commit payloads resolve receipt items against canonical food items and optional shopping row matches before persistence.\n- The client receives one success/failure boundary instead of three independent write outcomes.</behavior>
  <action>Turn `src/__tests__/receipt-food-flow.test.ts` into real assertions first, then implement the backend commit path. Create `supabase/migrations/00006_food_receipt_sync.sql` with any additional receipt-sync tables or SQL helpers needed for atomic writes, including a dedicated commit or audit table if useful. Implement `supabase/functions/commit-grocery-receipt/index.ts` so the reviewed OCR payload becomes one server-side workflow that creates the expense, writes inventory purchase events, and checks or links matching shopping rows per AIEX-04 and SYNC-01. Add `src/lib/receiptWorkflow.ts` as the client-side wrapper that assembles the reviewed commit payload; do not let `finances.tsx` orchestrate three separate writes.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/receipt-food-flow.test.ts</automated>
  </verify>
  <done>The one-photo-three-workflows save path exists behind one server-side commit boundary with green receipt-sync coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the receipt review UI and finances flow for food-domain matching</name>
  <files>src/hooks/useReceipt.ts, src/components/receipt/ReceiptReviewCard.tsx, src/app/(app)/finances.tsx, src/__tests__/receipt-food-flow.test.ts</files>
  <action>Update the existing Phase 2 receipt flow so grocery receipts keep the mandatory OCR review step, then add food-domain matching controls before save: canonical catalog resolution, inventory add/skip decisions, and optional shopping-list match selection. Wire `finances.tsx` to call `commitGroceryReceipt` from `src/lib/receiptWorkflow.ts` instead of `createExpense` directly when the reviewed receipt is a grocery flow. Preserve the existing item classification review UX from Phase 2; extend it rather than replacing it.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/receipt-food-flow.test.ts</automated>
  </verify>
  <done>Users can confirm a grocery receipt once and reliably get the expense, pantry updates, and shopping checkoffs together after a mandatory review step.</done>
</task>

</tasks>

<verification>
Run the receipt food-flow test suite and confirm the finances receipt path now calls the atomic grocery commit wrapper instead of direct expense creation.
</verification>

<success_criteria>
Phase 4 includes the “one receipt photo, three workflows” moment with transactional integrity and no client-side partial-write choreography.
</success_criteria>

<output>
After completion, create `.planning/phases/04-shopping-meals-supplies/04-07-SUMMARY.md`
</output>
