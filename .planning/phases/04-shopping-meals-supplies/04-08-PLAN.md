---
phase: 04-shopping-meals-supplies
plan: "08"
type: execute
wave: 6
depends_on:
  - "04-05"
  - "04-06"
files_modified:
  - supabase/functions/generate-meal-plan/index.ts
  - src/lib/mealPlanning.ts
  - src/hooks/useMealPlans.ts
  - src/components/meals/AIMealPlanSheet.tsx
  - src/app/(app)/meals.tsx
  - src/types/recipes.ts
  - src/types/meals.ts
  - src/__tests__/food-ai.test.ts
  - src/__tests__/meals-ui.test.ts
autonomous: true
requirements:
  - MEAL-07
  - AIML-01
  - AIML-04
  - AIML-05
  - AISH-02
  - SYNC-03
  - SYNC-07
gap_closure: true
must_haves:
  truths:
    - "AI meal recommendations visibly factor pantry fit, past accepted or cooked meals, and household favorites instead of feeling generic."
    - "Manually added dishes can rotate back into recommendations after recent use cools off, rather than disappearing after first save."
    - "Recommended dishes explain why they fit this week, including pantry overlap, household preference signals, or recent-history rotation cues."
  artifacts:
    - path: "supabase/functions/generate-meal-plan/index.ts"
      provides: "History-aware meal recommendation ranking and manually added dish rotation in suggestion runs."
      contains: "rotation"
    - path: "src/lib/mealPlanning.ts"
      provides: "Pure helpers for preference, recency, pantry-fit, and favorite-dish recommendation scoring."
      contains: "buildRecommendation"
    - path: "src/components/meals/AIMealPlanSheet.tsx"
      provides: "Meal suggestion review UI that surfaces ranking reasons and rotation context."
      contains: "Why this fits"
    - path: "src/hooks/useMealPlans.ts"
      provides: "Meal history and feedback loading shaped for recommendation reuse."
      contains: "meal_suggestion_feedback"
  key_links:
    - from: "src/app/(app)/meals.tsx"
      to: "supabase/functions/generate-meal-plan/index.ts"
      via: "planner request includes pantry, favorites, accepted feedback, and recent cooked history"
      pattern: "generate-meal-plan"
    - from: "src/hooks/useMealPlans.ts"
      to: "src/lib/mealPlanning.ts"
      via: "loaded meal history and feedback are transformed into recommendation inputs instead of UI-only data"
      pattern: "feedback|cooked|history"
    - from: "src/components/meals/AIMealPlanSheet.tsx"
      to: "src/types/meals.ts"
      via: "suggestion cards render structured explanation fields rather than inferring rationale from tags alone"
      pattern: "recommendation|rotation|pantry"
---

<objective>
Close the Phase 04 UAT gap around meal-history-driven recommendations by making AI meal suggestions feel household-aware: pantry-fit, preference-backed, and able to rotate manual dishes back into the week.

Purpose: Test 4 passed the recipe and board mechanics but failed the recommendation quality bar. This follow-up plan fixes the missing synergy between meals, pantry, grocery generation, and household history without replacing the existing Phase 04 architecture.
Output: One history-aware recommendation flow spanning the planner helper layer, meal-plan hook loading, AI meal suggestion contract, and meals review UI.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-shopping-meals-supplies/04-UAT.md
@.planning/phases/04-shopping-meals-supplies/04-05-SUMMARY.md
@.planning/phases/04-shopping-meals-supplies/04-06-SUMMARY.md
@supabase/functions/generate-meal-plan/index.ts
@src/lib/mealPlanning.ts
@src/hooks/useMealPlans.ts
@src/components/meals/AIMealPlanSheet.tsx
@src/app/(app)/meals.tsx

<interfaces>
From src/types/recipes.ts:
```typescript
export interface Recipe {
  id: string;
  title: string;
  source: 'manual' | 'url_import' | 'ai_import';
  tags: string[];
  favorite: boolean;
  servings: number | null;
}
```

From src/types/meals.ts:
```typescript
export interface MealSuggestion {
  id: string;
  suggestionRunId: string;
  recipeId: string | null;
  title: string;
  slot: MealPlanSlot;
  plannedForDate: string;
  rationale: string | null;
  servings: number;
  attendanceMemberIds: string[];
  estimatedCostCents: number | null;
  tags: string[];
  rank: number;
}

export interface MealSuggestionFeedback {
  suggestionRunId: string;
  suggestionId: string | null;
  mealPlanEntryId: string | null;
  action: 'accept' | 'swap' | 'regenerate' | 'reject';
  recipeId: string | null;
}
```

From src/hooks/useMealPlans.ts:
```typescript
export function useMealPlans() {
  return {
    mealPlans,
    suggestionRuns,
    suggestions,
    feedback,
    saveMealPlanEntry,
    generateShoppingList,
    markMealCooked,
    submitSuggestionFeedback,
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add history-aware recommendation scoring and manual-dish rotation contracts</name>
  <files>supabase/functions/generate-meal-plan/index.ts, src/lib/mealPlanning.ts, src/hooks/useMealPlans.ts, src/types/recipes.ts, src/types/meals.ts, src/__tests__/food-ai.test.ts</files>
  <behavior>
    - Test 1: accepted or cooked meals increase preference weight for matching recipes, but very recent repeats are penalized so the same dish does not dominate consecutive weeks.
    - Test 2: manual recipes and favorites remain eligible recommendations and can rotate back in when pantry fit and recency make them a good match.
    - Test 3: suggestion output includes structured explanation data for pantry fit, household-favorite status, and rotation reason instead of relying on placeholder tags.
  </behavior>
  <action>Expand `src/__tests__/food-ai.test.ts` first to cover the failed UAT truth: recommendation ranking must combine pantry-fit, accepted/cooked feedback, favorite/manual recipe signals, and recency cooldown. Then implement the minimal pure helper layer in `src/lib/mealPlanning.ts` to build recommendation inputs and ranking outputs, reusing existing planner payload shaping rather than inventing a second planner model. Update `src/hooks/useMealPlans.ts` to expose the recent meal history and feedback needed by the planner call, and tighten `src/types/meals.ts` / `src/types/recipes.ts` only where structured recommendation fields are needed. Finally, replace the placeholder ranking in `supabase/functions/generate-meal-plan/index.ts` so suggestion order and explanation fields come from the helper output, not index order. Preserve the existing review-first suggestion flow and JSON-backed suggestion run persistence from 04-06.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/food-ai.test.ts</automated>
  </verify>
  <done>Meal suggestion runs are ranked by real household signals, and suggestion payloads include enough structured explanation data to prove why a manual or favorite dish resurfaced.</done>
</task>

<task type="auto">
  <name>Task 2: Surface rotation reasons and pantry or grocery synergy in the meals review UX</name>
  <files>src/components/meals/AIMealPlanSheet.tsx, src/app/(app)/meals.tsx, src/hooks/useMealPlans.ts, src/__tests__/meals-ui.test.ts</files>
  <action>Update the meals screen and AI sheet so recommendation cards explicitly show why a suggestion fits this week: pantry matches, household favorite or accepted-history signal, manual-dish rotation, and any repeat cooldown context. When generating the week, pass the existing pantry-aware shopping context and the newly loaded meal history into the planner request so the synergy is real, not decorative. Keep the current per-slot accept, swap, and regenerate behavior, but make manually added dishes and favorites easy to recognize as rotating household staples. Reuse the existing `useMealPlans` and `AIMealPlanSheet` architecture; do not add a second meal recommendation surface.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/food-ai.test.ts src/__tests__/meals-ui.test.ts</automated>
  </verify>
  <done>The meals UI shows recommendation reasons tied to pantry, preferences, and history, and a user can see manually added dishes rotating back as justified weekly suggestions instead of generic AI picks.</done>
</task>

</tasks>

<verification>
Re-run the focused food AI and meals UI suites, then confirm the generated suggestions no longer rely on index-based ranking or placeholder pantry/history tags alone.
</verification>

<success_criteria>
Phase 04 closes the UAT gap when weekly meal suggestions clearly reflect household cooking history, pantry state, and favorite manual dishes, while keeping the existing meal board, shopping generation, and review-first AI flow intact.
</success_criteria>

<output>
After completion, create `.planning/phases/04-shopping-meals-supplies/04-08-SUMMARY.md`
</output>
