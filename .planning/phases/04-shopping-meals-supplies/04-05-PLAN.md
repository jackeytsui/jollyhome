---
phase: 04-shopping-meals-supplies
plan: "05"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - supabase/functions/import-recipe/index.ts
  - src/app/(app)/meals.tsx
  - src/components/meals/RecipeEditorSheet.tsx
  - src/components/meals/RecipeImportSheet.tsx
  - src/components/meals/RecipeCard.tsx
  - src/components/meals/MealBoard.tsx
  - src/components/meals/MealSlotCard.tsx
  - src/components/meals/MealPlanReviewSheet.tsx
  - src/__tests__/meals-ui.test.ts
autonomous: true
requirements:
  - SHOP-09
  - MEAL-01
  - MEAL-02
  - MEAL-03
  - MEAL-04
  - MEAL-05
  - MEAL-06
  - MEAL-07
  - SYNC-03
  - SYNC-04
  - SYNC-07
must_haves:
  truths:
    - "Users can create and import recipes, save favorites and tags, and manage them without leaving the meal-planning surface."
    - "Meal planning uses a dedicated weekly board for drag-and-drop authoring, then projects the results into the shared calendar."
    - "Meal plans can generate shopping needs with pantry deduction and deduct ingredients when marked cooked."
  artifacts:
    - path: "supabase/functions/import-recipe/index.ts"
      provides: "Structured recipe import with JSON-LD first, Cheerio fallback, and manual review data."
      contains: "schema.org"
    - path: "src/app/(app)/meals.tsx"
      provides: "Weekly meal-planning surface with recipe library and drag-and-drop authoring."
      contains: "MealBoard"
    - path: "src/components/meals/MealPlanReviewSheet.tsx"
      provides: "Review/apply controls for shopping generation and cooked-meal completion."
      contains: "Generate shopping list"
  key_links:
    - from: "src/app/(app)/meals.tsx"
      to: "src/hooks/useMealPlans.ts"
      via: "weekly board state, shopping generation, and cooked-meal actions"
      pattern: "useMealPlans"
    - from: "src/components/meals/RecipeImportSheet.tsx"
      to: "supabase/functions/import-recipe/index.ts"
      via: "URL import preview and manual correction flow"
      pattern: "import-recipe"
    - from: "src/components/meals/MealBoard.tsx"
      to: "react-native-draggable-flatlist"
      via: "drag-and-drop meal authoring outside the calendar renderer"
      pattern: "DraggableFlatList"
---

<objective>
Ship the recipes and meal-planning surface: recipe CRUD/import, favorites/tags, weekly drag-and-drop authoring, pantry-aware shopping generation, and cooked-meal deductions.

Purpose: This delivers the second major Phase 4 surface and proves the meal domain stays separate from the calendar source of truth while still projecting into it.
Output: `meals.tsx`, recipe and meal-planning components, `import-recipe` Edge Function, and green meal UI tests.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-shopping-meals-supplies/04-RESEARCH.md
@.planning/phases/04-shopping-meals-supplies/04-03-PLAN.md
@.planning/phases/03-chores-calendar/03-07-SUMMARY.md
@src/app/(app)/calendar.tsx
@src/components/calendar/CalendarAgendaList.tsx
@src/hooks/useAttendance.ts

<interfaces>
From src/hooks/useAttendance.ts:
```typescript
export function useAttendance();
// Attendance rows expose attendance_date and status: 'home_tonight' | 'away_tonight'
```

From src/types/calendar.ts:
```typescript
export type CalendarActivityType = 'event' | 'meal' | 'maintenance' | 'guest' | 'booking' | 'quiet_hours';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build recipe CRUD and URL import with manual review</name>
  <files>supabase/functions/import-recipe/index.ts, src/components/meals/RecipeEditorSheet.tsx, src/components/meals/RecipeImportSheet.tsx, src/components/meals/RecipeCard.tsx, src/__tests__/meals-ui.test.ts</files>
  <behavior>- Users can add recipes manually with ingredients, instructions, photos, favorites, and tags.\n- Recipe import prefers structured recipe data from the target URL, falls back to HTML parsing, and always ends in a manual review/edit step before saving.\n- Recipe metadata stays in the recipe domain, not inside calendar-event blobs.</behavior>
  <action>Turn the recipe sections of `src/__tests__/meals-ui.test.ts` into real assertions first, then implement the recipe library surface. Create `supabase/functions/import-recipe/index.ts` to fetch a URL, parse JSON-LD/schema.org Recipe data first, then use `cheerio` as a fallback. Return a reviewable draft instead of auto-saving imported content per MEAL-03 and the research guidance. Build sheet-based recipe create/edit/import components and a reusable recipe card that exposes favorites/tags per MEAL-02 and MEAL-07. Reuse Phase 1 dietary preference data through existing member/profile hooks instead of inventing a second preference store per MEAL-05.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/meals-ui.test.ts</automated>
  </verify>
  <done>The recipe layer supports manual authoring, URL import with review, favorites, tags, and dietary metadata inputs needed by meal planning.</done>
</task>

<task type="auto">
  <name>Task 2: Build the weekly meal board, shopping generation, and cooked-meal flow</name>
  <files>src/app/(app)/meals.tsx, src/components/meals/MealBoard.tsx, src/components/meals/MealSlotCard.tsx, src/components/meals/MealPlanReviewSheet.tsx, src/__tests__/meals-ui.test.ts</files>
  <action>Create the weekly meal-planning screen around a dedicated `react-native-draggable-flatlist` meal board, not the calendar renderer, per the Phase 4 research. Users must be able to drag meals across days/slots per MEAL-01, generate shopping rows with pantry deduction per SHOP-09 / MEAL-04 / SYNC-03, and mark meals cooked to deduct ingredients via `mark_meal_cooked` per SYNC-04. Use Phase 3 attendance data directly to calculate servings and display who is home on each day per MEAL-06 / SYNC-07. Keep the shared calendar as a projection surface only; do not write meals into `calendar_events` as source of truth.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/meals-ui.test.ts</automated>
  </verify>
  <done>The meal planner provides drag-and-drop weekly authoring, attendance-based servings, pantry-aware shopping generation, and cooked-meal inventory deduction without abusing the calendar schema.</done>
</task>

</tasks>

<verification>
Run the meals UI test suite and confirm the meal board, recipe import flow, and meal-plan actions route through the Phase 4 hooks and RPCs.
</verification>

<success_criteria>
Users can manage recipes and weekly meal plans end-to-end, with shopping and pantry impacts wired through the shared Phase 4 foundation.
</success_criteria>

<output>
After completion, create `.planning/phases/04-shopping-meals-supplies/04-05-SUMMARY.md`
</output>
