---
phase: 04-shopping-meals-supplies
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - supabase/migrations/00005_food_domain.sql
  - src/lib/foodNormalization.ts
  - src/lib/pantryDiff.ts
  - src/lib/mealPlanning.ts
  - src/__tests__/food-core.test.ts
  - src/__tests__/food-ai.test.ts
autonomous: true
requirements:
  - SHOP-05
  - SHOP-06
  - SHOP-07
  - SHOP-09
  - MEAL-04
  - MEAL-06
  - AIML-01
  - AIML-02
  - AIML-05
  - AISH-01
  - AISH-02
  - SYNC-03
  - SYNC-04
  - SYNC-05
  - SYNC-07
must_haves:
  truths:
    - "The food domain persists canonical catalog items, shopping lists, pantry stock, recipes, meal plans, AI suggestion runs, and low-stock alerts with household-scoped access rules."
    - "Shared normalization, pantry-diff, and meal-planning helpers exist before any Phase 4 screen hand-rolls matching, deduction, or suggestion logic."
    - "Threshold restock, cooked-meal deduction, and attendance-aware meal planning all have one reusable data foundation instead of per-screen heuristics."
  artifacts:
    - path: "supabase/migrations/00005_food_domain.sql"
      provides: "Phase 4 schema, RLS, indexes, and RPC helpers for food-domain persistence."
      contains: "CREATE TABLE public.shopping_lists"
    - path: "src/lib/pantryDiff.ts"
      provides: "Needed-vs-on-hand calculations for recipe import and meal-plan shopping generation."
      contains: "buildPantryDiff"
    - path: "src/lib/mealPlanning.ts"
      provides: "Planner input contracts and ingredient-overlap scoring helpers."
      contains: "scoreIngredientOverlap"
  key_links:
    - from: "src/lib/foodNormalization.ts"
      to: "supabase/migrations/00005_food_domain.sql"
      via: "canonical item/category keys must match the stored catalog and shopping schemas"
      pattern: "categoryKey|canonicalName"
    - from: "src/lib/pantryDiff.ts"
      to: "src/types/recipes.ts"
      via: "recipe ingredients resolve through normalized catalog quantities"
      pattern: "RecipeIngredient|catalogItemId"
    - from: "src/lib/mealPlanning.ts"
      to: "src/types/meals.ts"
      via: "AI suggestion inputs and feedback outputs stay aligned with the meal-plan contracts"
      pattern: "MealSuggestion|MealSuggestionFeedback"
---

<objective>
Create the real Phase 4 foundation: the food-domain schema and pure libraries that normalize item identity, calculate pantry deltas, and build AI meal-planning inputs before hooks or screens are added.

Purpose: This isolates the highest-risk data modeling work so every later plan reuses one canonical source of truth.
Output: `00005_food_domain.sql`, `foodNormalization.ts`, `pantryDiff.ts`, `mealPlanning.ts`, and green core food tests.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-shopping-meals-supplies/04-RESEARCH.md
@.planning/phases/04-shopping-meals-supplies/04-01-PLAN.md
@supabase/migrations/00003_expenses.sql
@supabase/migrations/00004_chores_calendar.sql

<interfaces>
From src/types/inventory.ts:
```typescript
export interface FoodCatalogItem {
  id: string;
  canonicalName: string;
  categoryKey: string;
  defaultUnit: string;
  barcode: string | null;
}

export interface InventoryEvent {
  inventoryItemId: string;
  deltaQuantity: number;
  sourceType: 'receipt_purchase' | 'meal_cooked' | 'manual_adjustment' | 'pantry_scan_seed' | 'restock_prediction';
}
```

From src/types/meals.ts:
```typescript
export interface MealPlanEntry {
  id: string;
  recipeId: string | null;
  slotDate: string;
  slotKey: 'breakfast' | 'lunch' | 'dinner';
  servingsPlanned: number;
  status: 'planned' | 'cooked' | 'skipped';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 4 food schema, RLS, and core RPC helpers</name>
  <files>supabase/migrations/00005_food_domain.sql</files>
  <action>Create `supabase/migrations/00005_food_domain.sql` following the existing top-level DDL pattern. Add tables for `food_catalog_items`, `shopping_lists`, `shopping_list_items`, `inventory_items`, `inventory_events`, `inventory_alerts`, `recipes`, `recipe_ingredients`, `meal_plan_entries`, `meal_suggestion_runs`, and `meal_suggestion_feedback`. Use canonical catalog references everywhere free-text matching would break SHOP-05, SHOP-09, and SYNC-01. Add household-scoped RLS policies, indexes on household/date/status/category fields, and RPC helpers `upsert_inventory_event`, `generate_meal_plan_shopping_list`, and `mark_meal_cooked` so threshold evaluation, pantry deduction, and shopping rollups run server-side per SYNC-03, SYNC-04, and SYNC-05. Reuse Phase 1 profile dietary fields and Phase 3 attendance rows rather than adding duplicate preference or attendance tables per MEAL-05 and SYNC-07.</action>
  <verify>
    <automated>rg -n "CREATE TABLE public\\.(food_catalog_items|shopping_lists|shopping_list_items|inventory_items|inventory_events|inventory_alerts|recipes|recipe_ingredients|meal_plan_entries|meal_suggestion_runs|meal_suggestion_feedback)|CREATE OR REPLACE FUNCTION public\\.(upsert_inventory_event|generate_meal_plan_shopping_list|mark_meal_cooked)|ENABLE ROW LEVEL SECURITY|REFERENCES public\\.food_catalog_items" supabase/migrations/00005_food_domain.sql</automated>
  </verify>
  <done>The database can persist canonical food identity, shopping lists, pantry stock history, recipes, meal plans, AI suggestion runs, and low-stock alerts with household isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement normalization, pantry diff, and meal-planning core helpers</name>
  <files>src/lib/foodNormalization.ts, src/lib/pantryDiff.ts, src/lib/mealPlanning.ts, src/__tests__/food-core.test.ts, src/__tests__/food-ai.test.ts</files>
  <behavior>- Normalization groups shopping, pantry, recipe, and receipt references through one canonical name/category/unit layer instead of free-text matching.\n- Pantry diff rolls recipe ingredients and meal-plan demand against on-hand inventory so generated shopping rows include only missing quantities.\n- Meal-planning helpers derive attendance-aware serving counts, prep-time buckets, and ingredient-overlap scores for later AI plans.</behavior>
  <action>Turn the scaffold suites in `src/__tests__/food-core.test.ts` and `src/__tests__/food-ai.test.ts` into real assertions first, then implement the corresponding helpers. `src/lib/foodNormalization.ts` must export canonical category maps, alias resolution, aisle grouping, and unit-normalization helpers per SHOP-05. `src/lib/pantryDiff.ts` must export `buildPantryDiff` and meal-week rollup helpers that support recipe-to-shopping generation and cooked-meal deductions per SHOP-09, MEAL-04, and SYNC-03 / SYNC-04. `src/lib/mealPlanning.ts` must export planner input builders, ingredient-overlap scoring for AIML-02, attendance-driven serving calculation per MEAL-06 / SYNC-07, and prep-time availability bucketing per AIML-05. Keep these helpers pure and reusable; do not move UI or Supabase calls into them.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/food-core.test.ts src/__tests__/food-ai.test.ts</automated>
  </verify>
  <done>The shared food-domain helpers are implemented behind green tests and later hooks or Edge Functions can reuse them without re-deriving matching or deduction logic.</done>
</task>

</tasks>

<verification>
Confirm the Phase 4 schema exposes the required food tables and RPCs, then run the food core and AI helper tests together.
</verification>

<success_criteria>
Phase 4 has one normalized data model and one reusable pantry/meal logic layer before any screen starts writing its own cross-feature rules.
</success_criteria>

<output>
After completion, create `.planning/phases/04-shopping-meals-supplies/04-02-SUMMARY.md`
</output>
