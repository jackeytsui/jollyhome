---
phase: 04-shopping-meals-supplies
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/hooks/useFoodRealtime.ts
  - src/hooks/useShopping.ts
  - src/hooks/useInventory.ts
  - src/hooks/useRecipes.ts
  - src/hooks/useMealPlans.ts
  - src/lib/calendarProjection.ts
  - src/hooks/useCalendar.ts
  - src/__tests__/food-hooks.test.ts
autonomous: true
requirements:
  - SHOP-01
  - SHOP-03
  - SHOP-06
  - SHOP-07
  - SHOP-09
  - MEAL-02
  - MEAL-04
  - MEAL-06
  - MEAL-07
  - SYNC-03
  - SYNC-04
  - SYNC-05
  - SYNC-07
must_haves:
  truths:
    - "Shopping, inventory, recipes, and meal plans share one household realtime refresh pattern instead of separate refetch storms."
    - "Meal-plan entries project into the existing calendar data flow as real meal timeline items rather than a second calendar store."
    - "The base hooks expose food-domain load and mutation helpers so later screens do not issue raw Supabase calls."
  artifacts:
    - path: "src/hooks/useFoodRealtime.ts"
      provides: "Shared household food channel orchestration for the Phase 4 hooks."
      contains: "household:${activeHouseholdId}:food"
    - path: "src/hooks/useShopping.ts"
      provides: "Shopping list load and mutation helpers with checked-item ordering support."
      contains: "toggleItemChecked"
    - path: "src/hooks/useMealPlans.ts"
      provides: "Meal-plan, shopping-rollup, and cooked-meal mutation helpers."
      contains: "markMealCooked"
  key_links:
    - from: "src/hooks/useMealPlans.ts"
      to: "supabase/migrations/00005_food_domain.sql"
      via: "meal-plan generation and cooked-meal RPC calls"
      pattern: "generate_meal_plan_shopping_list|mark_meal_cooked"
    - from: "src/hooks/useCalendar.ts"
      to: "src/lib/calendarProjection.ts"
      via: "meal_plan_entries are projected into calendar items before rendering"
      pattern: "sourceType: 'meal'|mealPlanEntries"
    - from: "src/hooks/useFoodRealtime.ts"
      to: "src/hooks/useShopping.ts"
      via: "single household food channel fan-out for shopping and pantry refresh"
      pattern: "refreshShopping|refreshInventory|refreshRecipes|refreshMealPlans"
---

<objective>
Create the reusable Phase 4 client foundation: one shared realtime channel, food-domain hooks, and meal projection wiring into the existing calendar pipeline.

Purpose: This prevents every food screen from duplicating Supabase access patterns and keeps Phase 4 aligned with the Phase 3 projection architecture.
Output: `useFoodRealtime`, `useShopping`, `useInventory`, `useRecipes`, `useMealPlans`, and meal-aware calendar projection updates.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-shopping-meals-supplies/04-RESEARCH.md
@.planning/phases/04-shopping-meals-supplies/04-02-PLAN.md
@.planning/phases/03-chores-calendar/03-05-SUMMARY.md
@.planning/phases/03-chores-calendar/03-07-SUMMARY.md
@src/hooks/useCalendar.ts
@src/lib/calendarProjection.ts

<interfaces>
From src/hooks/useCalendar.ts:
```typescript
const REALTIME_CHANNEL_NAME = (householdId: string) => `household:${householdId}:chores-calendar`;
export const CALENDAR_SOURCE_ICON_MAP: Record<HouseholdCalendarItem['sourceType'], string>;
export function useCalendar();
```

From src/lib/calendarProjection.ts:
```typescript
export function projectCalendarItems(input: ProjectCalendarItemsInput): HouseholdCalendarItem[];
export function groupAgendaItemsByDay(items: HouseholdCalendarItem[]): AgendaGroup[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the shared food realtime orchestrator and base hooks</name>
  <files>src/hooks/useFoodRealtime.ts, src/hooks/useShopping.ts, src/hooks/useInventory.ts, src/hooks/useRecipes.ts, src/hooks/useMealPlans.ts, src/__tests__/food-hooks.test.ts</files>
  <behavior>- All Phase 4 hooks refresh through one `household:{id}:food` channel instead of subscribing independently and storming the network.\n- Shopping hooks preserve checked-at ordering and active-list semantics, inventory hooks expose threshold and adjustment flows, recipe hooks expose CRUD/import helpers, and meal hooks expose shopping-rollup plus cooked-meal actions.\n- Hook public APIs stay consistent with the repo pattern: local `useState`, async loaders/mutators, reload-after-mutation, and explicit loading/error state.</behavior>
  <action>Convert `src/__tests__/food-hooks.test.ts` from scaffold to real coverage first, then implement the five hooks and the shared `useFoodRealtime` orchestrator. Follow the established Phase 2 and Phase 3 hook pattern: `useState` data/loading/error, `useCallback` loaders, `useEffect` mount + realtime subscription, and mutation helpers that reload on success. Use one channel name shaped exactly like `household:${activeHouseholdId}:food` per the Phase 4 research. `useShopping` must include active list selection, item CRUD, category grouping, and checked-item toggling per SHOP-01 through SHOP-04. `useInventory` must expose stock adjustments, threshold updates, low-stock alert reads, and barcode/manual item resolution per SHOP-06 / SHOP-07 / SYNC-05. `useRecipes` must expose recipe CRUD, favorites, tags, and import-draft persistence per MEAL-02 and MEAL-07. `useMealPlans` must expose weekly load/save, shopping-list generation via `generate_meal_plan_shopping_list`, and cooked-meal completion via `mark_meal_cooked` per MEAL-04 and SYNC-04.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/food-hooks.test.ts</automated>
  </verify>
  <done>The base Phase 4 hooks expose household-scoped data and mutations behind one coordinated food realtime pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Project meal-plan entries into the shared calendar pipeline</name>
  <files>src/lib/calendarProjection.ts, src/hooks/useCalendar.ts</files>
  <action>Extend the existing calendar projection flow so `meal_plan_entries` load through `useCalendar` and project into `HouseholdCalendarItem` rows with `sourceType: 'meal'`, consistent icon mapping, and metadata that meal screens can round-trip. Keep the source of truth in the meal tables, not `calendar_events`, per the Phase 4 research and D-avoid-overloading-calendar guidance. Reuse Phase 3 projection helpers rather than creating a second calendar renderer. Meal items must respect attendance-derived servings and recipe references in metadata so MEAL-06 and SYNC-07 stay wired into the unified household timeline.</action>
  <verify>
    <automated>rg -n "meal_plan_entries|sourceType: 'meal'|CALENDAR_SOURCE_ICON_MAP\\['meal'\\]|attendanceMemberIds|recipeId" src/hooks/useCalendar.ts src/lib/calendarProjection.ts</automated>
  </verify>
  <done>The existing calendar data pipeline can render meals as projected timeline items without moving meal state into generic calendar events.</done>
</task>

</tasks>

<verification>
Run the food hook test suite and confirm the calendar projection source now loads and maps meal-plan entries through the existing projection layer.
</verification>

<success_criteria>
Phase 4 screens can build on stable hooks and calendar integration instead of screen-local Supabase access or a separate meal calendar architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/04-shopping-meals-supplies/04-03-SUMMARY.md`
</output>
