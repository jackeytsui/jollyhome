---
phase: 04-shopping-meals-supplies
plan: "06"
type: execute
wave: 5
depends_on:
  - "04-04"
  - "04-05"
files_modified:
  - supabase/functions/generate-meal-plan/index.ts
  - supabase/functions/identify-pantry-items/index.ts
  - src/lib/mealPlanning.ts
  - src/components/meals/AIMealPlanSheet.tsx
  - src/components/meals/MealSuggestionCard.tsx
  - src/components/supplies/PantryPhotoReviewSheet.tsx
  - src/app/(app)/meals.tsx
  - src/app/(app)/supplies.tsx
  - src/__tests__/food-ai.test.ts
autonomous: true
requirements:
  - AIML-01
  - AIML-02
  - AIML-03
  - AIML-04
  - AIML-05
  - AISH-01
  - AISH-02
  - AISH-03
  - SYNC-05
  - SYNC-07
user_setup:
  - service: openai
    why: "Edge Functions generate AI meal plans and pantry-photo item identification"
    env_vars:
      - name: OPENAI_API_KEY
        source: "Supabase Edge Function secret already used by Phase 2 OCR and NL parsing functions"
must_haves:
  truths:
    - "AI can generate weekly meal-plan suggestions using pantry contents, preferences, attendance, calendar load, budget, and prep-time constraints."
    - "Users can accept, swap, or regenerate individual meal suggestions and the system records feedback for future learning."
    - "Pantry photo analysis and low-stock prediction produce reviewable, inventory-aware restock suggestions rather than silent background mutations."
  artifacts:
    - path: "supabase/functions/generate-meal-plan/index.ts"
      provides: "AI meal-plan generation with suggestion-run persistence and structured output."
      contains: "acceptedIds"
    - path: "supabase/functions/identify-pantry-items/index.ts"
      provides: "Pantry/fridge photo item identification draft generation."
      contains: "review_items"
    - path: "src/components/meals/AIMealPlanSheet.tsx"
      provides: "Accept, swap, regenerate, and feedback review UI for AI meal suggestions."
      contains: "Regenerate"
  key_links:
    - from: "src/app/(app)/meals.tsx"
      to: "supabase/functions/generate-meal-plan/index.ts"
      via: "meal planner sends one structured request and stores feedback actions"
      pattern: "generate-meal-plan"
    - from: "src/app/(app)/supplies.tsx"
      to: "supabase/functions/identify-pantry-items/index.ts"
      via: "pantry photo capture opens a review sheet before inventory writes"
      pattern: "identify-pantry-items"
    - from: "src/lib/mealPlanning.ts"
      to: "src/components/meals/MealSuggestionCard.tsx"
      via: "ingredient-overlap, prep-time, and attendance rationale is displayed in UI"
      pattern: "ingredientOverlap|prepTimeBucket|attendanceMemberIds"
---

<objective>
Add the AI layer to the food domain: weekly AI meal suggestions, per-slot accept/swap/regenerate feedback, predictive replenishment, and pantry-photo identification with manual review.

Purpose: This is the differentiator plan for Phase 4 and it builds strictly on the already-wired shopping, inventory, recipe, and meal foundations.
Output: AI meal-planning and pantry-photo Edge Functions, AI review UI on meals/supplies screens, and green food AI tests.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-shopping-meals-supplies/04-RESEARCH.md
@.planning/phases/04-shopping-meals-supplies/04-04-PLAN.md
@.planning/phases/04-shopping-meals-supplies/04-05-PLAN.md
@src/hooks/useReceipt.ts
@src/hooks/useAttendance.ts
@src/hooks/useCalendar.ts

<interfaces>
From src/hooks/useAttendance.ts:
```typescript
export function useAttendance();
```

From src/hooks/useCalendar.ts:
```typescript
export function useCalendar();
// Calendar events expose availability / busyness inputs that can influence meal prep-time suggestions.
```

From src/types/meals.ts:
```typescript
export interface MealSuggestionFeedback {
  mealPlanEntryId: string | null;
  action: 'accepted' | 'swapped' | 'regenerated' | 'rejected';
  reason: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AI weekly meal planning with accept, swap, regenerate, and feedback</name>
  <files>supabase/functions/generate-meal-plan/index.ts, src/lib/mealPlanning.ts, src/components/meals/AIMealPlanSheet.tsx, src/components/meals/MealSuggestionCard.tsx, src/app/(app)/meals.tsx, src/__tests__/food-ai.test.ts</files>
  <behavior>- AI suggestion runs use recipes, pantry state, dietary preferences, attendance, budget, and calendar load as structured inputs.\n- Users can accept, swap, or regenerate individual meal slots without discarding the whole week.\n- Feedback events persist so later runs can learn from accepted and rejected suggestions.</behavior>
  <action>Turn the AI meal-planning sections of `src/__tests__/food-ai.test.ts` into real assertions first, then implement the AI planner flow. `supabase/functions/generate-meal-plan/index.ts` must build one structured prompt/request from normalized meal-planning inputs and return explicit slot suggestions, ingredient-overlap rationale per AIML-02, and prep-time rationale per AIML-05. Update `src/lib/mealPlanning.ts` only where needed to serialize planner inputs and derive rationale copy. Build `AIMealPlanSheet` and `MealSuggestionCard` so users can accept, swap, or regenerate individual suggestions per AIML-03, and persist `meal_suggestion_feedback` rows per AIML-04. Use attendance directly from Phase 3 and do not introduce a parallel attendance store per SYNC-07.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/food-ai.test.ts</automated>
  </verify>
  <done>The meal planner can generate AI weekly plans with structured rationale and per-slot accept/swap/regenerate flows backed by persisted feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Implement low-stock prediction and pantry-photo identification with review</name>
  <files>supabase/functions/identify-pantry-items/index.ts, src/components/supplies/PantryPhotoReviewSheet.tsx, src/app/(app)/supplies.tsx, src/__tests__/food-ai.test.ts</files>
  <action>Create `identify-pantry-items` to analyze pantry/fridge photos into reviewable candidate items per AISH-03, then wire a `PantryPhotoReviewSheet` into `supplies.tsx` so users confirm edits before inventory writes. Use the inventory event ledger to surface low-stock predictions and proactive restock suggestions per AISH-01, and merge those suggestions with upcoming meal deficits for AISH-02 / SYNC-05. Keep pantry-photo and predictive restock flows review-first; no silent canonical writes from AI output.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/food-ai.test.ts</automated>
  </verify>
  <done>The supplies surface can suggest restocks from usage patterns and pantry-photo drafts, while still requiring user review before inventory changes become canonical.</done>
</task>

</tasks>

<verification>
Run the food AI test suite and confirm both AI Edge Functions are wired into the correct meals and supplies review flows.
</verification>

<success_criteria>
Phase 4 delivers its AI differentiators without bypassing the normalized food domain or hiding critical review steps from users.
</success_criteria>

<output>
After completion, create `.planning/phases/04-shopping-meals-supplies/04-06-SUMMARY.md`
</output>
