---
phase: 03-chores-calendar
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-05"
files_modified:
  - src/__tests__/calendar-ui.test.ts
  - src/components/calendar/EventEditorSheet.tsx
  - src/components/calendar/AttendanceToggleStrip.tsx
  - src/components/calendar/RSVPChips.tsx
  - src/components/calendar/CalendarLegend.tsx
  - src/hooks/useCalendar.ts
  - src/hooks/useAttendance.ts
autonomous: true
requirements:
  - CALD-01
  - CALD-02
  - CALD-03
  - CALD-04
  - CALD-05
  - CALD-06
must_haves:
  truths:
    - "Users can create household events, RSVP, and update home-tonight attendance without leaving the calendar surface."
    - "Per-member colors and event-type icon cues make event, chore, attendance, meal, maintenance, guest, quiet-hours, and booking items distinguishable before opening them."
    - "Attendance remains a lightweight status toggle instead of being conflated with event RSVP."
  artifacts:
    - path: "src/components/calendar/EventEditorSheet.tsx"
      provides: "Create/edit event flow with recurrence and member color assignment."
      contains: "Location"
    - path: "src/components/calendar/AttendanceToggleStrip.tsx"
      provides: "Home tonight / away tonight quick controls."
      contains: "Home tonight"
    - path: "src/components/calendar/CalendarLegend.tsx"
      provides: "Legend for member colors and event/chore/attendance icons."
      contains: "attendance"
  key_links:
    - from: "src/components/calendar/AttendanceToggleStrip.tsx"
      to: "src/hooks/useAttendance.ts"
      via: "same-day attendance upsert"
      pattern: "useAttendance\\("
    - from: "src/components/calendar/RSVPChips.tsx"
      to: "src/hooks/useCalendar.ts"
      via: "event RSVP updates"
      pattern: "updateRsvp|useCalendar"
    - from: "src/components/calendar/CalendarLegend.tsx"
      to: "src/types/calendar.ts"
      via: "iconKey and member-color cues for projected item types including future household activity overlays"
      pattern: "iconKey|primaryMemberId"
---

<objective>
Build the calendar control surfaces: event editor, RSVP chips, attendance toggles, and explicit icon/color cues for calendar item types.

Purpose: This keeps the control-layer work separate from full calendar rendering and fixes the missing CALD-03 icon planning.
Output: Event/RSVP/attendance components, icon mapping requirements, and calendar control tests.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-chores-calendar/03-CONTEXT.md
@.planning/phases/03-chores-calendar/03-RESEARCH.md
@.planning/phases/03-chores-calendar/03-05-PLAN.md
@src/hooks/useCalendar.ts
@src/hooks/useAttendance.ts
@src/types/calendar.ts
@src/constants/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build event, RSVP, attendance, and icon mapping controls on top of the shared hooks</name>
  <files>src/__tests__/calendar-ui.test.ts, src/components/calendar/EventEditorSheet.tsx, src/components/calendar/AttendanceToggleStrip.tsx, src/components/calendar/RSVPChips.tsx, src/components/calendar/CalendarLegend.tsx, src/hooks/useCalendar.ts, src/hooks/useAttendance.ts</files>
  <action>Create `EventEditorSheet` using the existing bottom-sheet pattern for create/edit event flows. It must capture title, start date/time, end date/time, location, description, recurrence rule, and event color/member association for CALD-01, CALD-02, and D-15. Add an `activityType` selector with the exact options `event`, `meal`, `maintenance`, `guest`, `quiet_hours`, and `booking`; default it to `event`. This gives Phase 3 a lightweight way to place the CALD-04 household activity types on the shared timeline before their later phases add richer domain-specific workflows. Create `RSVPChips` with the exact options `going`, `maybe`, and `not going`, mapped to the stored value `not_going` where needed per D-18. Create `AttendanceToggleStrip` with one-tap `Home tonight` and `Away tonight` actions that call `upsertAttendance` directly without routing users through the event form per D-17 and D-19. Update `useCalendar.ts` so projected items expose explicit icon mapping for `event`, `chore`, `attendance`, `meal`, `maintenance`, `guest`, `quiet-hours`, and `booking` types, and create `CalendarLegend` that renders both per-member colors and those event-type icons to satisfy CALD-03, CALD-04, and D-15.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/calendar-ui.test.ts</automated>
  </verify>
  <done>Calendar controls exist for event editing, lightweight CALD-04 activity-type entries, RSVP, attendance updates, and explicit multi-type icon cues.</done>
</task>

</tasks>

<verification>
Run the calendar UI test file and confirm the source contains event/chore/attendance icon mapping plus direct attendance actions.
</verification>

<success_criteria>
The calendar surface has the required create/edit/status controls and clear visual language before full calendar rendering is built.
</success_criteria>

<output>
After completion, create `.planning/phases/03-chores-calendar/03-03-SUMMARY.md`
</output>
