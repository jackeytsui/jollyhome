---
phase: 03-chores-calendar
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/__tests__/calendar-ui.test.ts
  - src/__tests__/calendar-projection.test.ts
  - src/app/(app)/calendar.tsx
  - src/components/calendar/CalendarViewSwitcher.tsx
  - src/components/calendar/EventEditorSheet.tsx
  - src/components/calendar/AttendanceToggleStrip.tsx
  - src/components/calendar/RSVPChips.tsx
  - src/components/calendar/CalendarLegend.tsx
  - src/components/calendar/CalendarAgendaList.tsx
  - src/hooks/useCalendar.ts
  - src/hooks/useAttendance.ts
autonomous: true
requirements:
  - CALD-01
  - CALD-02
  - CALD-03
  - CALD-04
  - CALD-05
  - CALD-06
  - CALD-07
must_haves:
  truths:
    - "Users can create events, RSVP, toggle home-tonight attendance, and view the household timeline in week, month, and agenda modes."
    - "Events remain the strongest visual anchor while chores and attendance render with lighter weight."
    - "Per-member colors and event-type cues make the calendar readable without opening every item."
  artifacts:
    - path: "src/app/(app)/calendar.tsx"
      provides: "Primary calendar screen with week/month/agenda views."
      contains: "Week"
    - path: "src/components/calendar/EventEditorSheet.tsx"
      provides: "Create/edit event flow with recurrence and RSVP support."
      contains: "Location"
    - path: "src/components/calendar/AttendanceToggleStrip.tsx"
      provides: "Home tonight / away tonight quick controls."
      contains: "Home tonight"
  key_links:
    - from: "src/app/(app)/calendar.tsx"
      to: "react-native-big-calendar"
      via: "week and month rendering"
      pattern: "from 'react-native-big-calendar'"
    - from: "src/app/(app)/calendar.tsx"
      to: "src/hooks/useCalendar.ts"
      via: "projected calendar items and event CRUD"
      pattern: "useCalendar\\("
    - from: "src/components/calendar/AttendanceToggleStrip.tsx"
      to: "src/hooks/useAttendance.ts"
      via: "same-day attendance upsert"
      pattern: "useAttendance\\("
---

<objective>
Build the calendar product surface: event creation and recurrence, RSVP, lightweight attendance toggles, and a readable household timeline using week as the primary view plus month and agenda.

Purpose: This plan delivers the shared-calendar half of Phase 3 while preserving D-13 through D-19 and the unified projection contract from Plan 01.
Output: Calendar screen, event/attendance components, and calendar-specific tests.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-chores-calendar/03-CONTEXT.md
@.planning/phases/03-chores-calendar/03-RESEARCH.md
@.planning/phases/03-chores-calendar/03-VALIDATION.md
@.planning/phases/03-chores-calendar/03-01-PLAN.md
@src/app/(app)/calendar.tsx
@src/hooks/useCalendar.ts
@src/hooks/useAttendance.ts
@src/lib/calendarProjection.ts
@src/types/calendar.ts
@src/constants/theme.ts

<interfaces>
From Plan 01:
```typescript
export interface HouseholdCalendarItem {
  id: string;
  sourceType: 'event' | 'chore' | 'attendance' | 'meal' | 'maintenance' | 'guest' | 'quiet-hours' | 'booking';
  title: string;
  startsAt: string;
  endsAt: string;
  primaryMemberId: string | null;
  visualWeight: 'primary' | 'secondary';
  iconKey: string;
}
```

```typescript
export function useAttendance(): {
  attendanceByDate: Record<string, AttendanceStatus>;
  upsertAttendance(...): Promise<boolean>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build event, RSVP, and attendance controls on top of the shared hooks</name>
  <files>src/__tests__/calendar-ui.test.ts, src/components/calendar/EventEditorSheet.tsx, src/components/calendar/AttendanceToggleStrip.tsx, src/components/calendar/RSVPChips.tsx, src/components/calendar/CalendarLegend.tsx, src/hooks/useCalendar.ts, src/hooks/useAttendance.ts</files>
  <read_first>src/hooks/useCalendar.ts, src/hooks/useAttendance.ts, src/types/calendar.ts, src/lib/recurrence.ts, src/lib/calendarProjection.ts, src/components/ui/Button.tsx, .planning/phases/03-chores-calendar/03-CONTEXT.md</read_first>
  <action>Create `EventEditorSheet` using the existing bottom-sheet pattern for create/edit event flows. It must capture the exact event fields title, start date/time, end date/time, location, description, recurrence rule, and event color/member association for CALD-01, CALD-02, and D-15. Create `RSVPChips` with the exact options `going`, `maybe`, and `not going`, mapped to the database value `not_going` where needed per D-18. Create `AttendanceToggleStrip` with one-tap `Home tonight` and `Away tonight` actions that call `upsertAttendance` directly without forcing users through the event form per D-17 and D-19. Create `CalendarLegend` showing member colors plus lighter styling for chores/attendance versus stronger styling for events per D-14 and D-15. Update `useCalendar.ts` and `useAttendance.ts` as needed so event CRUD, RSVP updates, and attendance upserts are all available to the screen layer.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/calendar-ui.test.ts</automated>
  </verify>
  <acceptance_criteria>- `src/components/calendar/EventEditorSheet.tsx` contains the exact strings `Location` and `Description`.\n- `src/components/calendar/RSVPChips.tsx` contains the strings `going`, `maybe`, and `not going`.\n- `src/components/calendar/AttendanceToggleStrip.tsx` contains the exact strings `Home tonight` and `Away tonight`.\n- `src/components/calendar/CalendarLegend.tsx` references both member colors and event/chore/attendance styling.\n- `src/__tests__/calendar-ui.test.ts` contains assertions for RSVP and attendance controls.\n- The automated test command exits successfully.</acceptance_criteria>
  <done>Calendar-specific controls exist for event editing, RSVP, and fast attendance updates without conflating attendance with RSVP.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the calendar stub with week-first household calendar rendering</name>
  <files>src/__tests__/calendar-projection.test.ts, src/app/(app)/calendar.tsx, src/components/calendar/CalendarViewSwitcher.tsx, src/components/calendar/CalendarAgendaList.tsx</files>
  <read_first>src/app/(app)/calendar.tsx, src/lib/calendarProjection.ts, src/hooks/useCalendar.ts, src/hooks/useAttendance.ts, src/types/calendar.ts, src/constants/theme.ts, .planning/phases/03-chores-calendar/03-RESEARCH.md</read_first>
  <action>Replace the current `src/app/(app)/calendar.tsx` stub with a full screen built around `react-native-big-calendar`. Week view must be the default selected mode per D-13, and the screen must also support month and agenda. Create `CalendarViewSwitcher` with explicit options `Week`, `Month`, and `Agenda`; if you ship day view as a secondary tab, keep it secondary and do not remove agenda. Use the projection layer so events render with `visualWeight: 'primary'` and chores/attendance render lighter per D-14 and D-16. Add `CalendarAgendaList` as the mobile-scannable fallback for agenda mode. Display per-member color coding and event-type cues on rendered items per D-15. Keep CALD-04 future-proof by allowing the screen to accept projection items for meals, maintenance, guests, quiet hours, and bookings even though Phase 3 only populates events, chores, and attendance sources.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/calendar-ui.test.ts src/__tests__/calendar-projection.test.ts</automated>
  </verify>
  <acceptance_criteria>- `src/app/(app)/calendar.tsx` imports from `react-native-big-calendar`.\n- `src/app/(app)/calendar.tsx` contains the exact strings `Week`, `Month`, and `Agenda`.\n- `src/app/(app)/calendar.tsx` imports or references `CalendarAgendaList`.\n- `src/app/(app)/calendar.tsx` references `projectCalendarItems` or the equivalent projection helper from `src/lib/calendarProjection.ts`.\n- `src/__tests__/calendar-projection.test.ts` contains assertions for projected source types `event`, `chore`, and `attendance` plus visual-weight behavior.\n- The automated test command exits successfully.</acceptance_criteria>
  <done>The calendar tab is a real household timeline with week/month/agenda support, readable density, and quick attendance/event actions.</done>
</task>

</tasks>

<verification>
Run both calendar test files. Confirm `src/app/(app)/calendar.tsx` no longer contains the Phase 1 stub and that the rendered modes and attendance/RSVP controls are present in source and tests.
</verification>

<success_criteria>
Users can treat the calendar as the shared household timeline: create recurring events, RSVP, mark home/away tonight quickly, and scan week, month, or agenda views without losing event prominence.
</success_criteria>

<output>
After completion, create `.planning/phases/03-chores-calendar/03-03-SUMMARY.md`
</output>
