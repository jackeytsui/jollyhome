---
phase: 03-chores-calendar
plan: "02"
type: execute
wave: 3
depends_on:
  - "03-05"
files_modified:
  - src/__tests__/chores-ui.test.ts
  - src/app/(app)/chores.tsx
  - src/components/chores/ChoreCard.tsx
  - src/components/chores/ChoreSection.tsx
  - src/components/chores/ChoreFiltersBar.tsx
  - src/components/chores/ChoreEditorSheet.tsx
  - src/components/chores/CompleteChoreSheet.tsx
autonomous: true
requirements:
  - CHOR-01
  - CHOR-02
  - CHOR-03
  - CHOR-04
  - CHOR-05
  - CHOR-06
  - CHOR-09
must_haves:
  truths:
    - "Users can create, assign, filter, claim, and complete chores from a shared household screen."
    - "The chores screen opens with a personal section first and keeps whole-household visibility below it."
    - "Condition bars and completion confirmation make urgency and completion status readable without clutter."
  artifacts:
    - path: "src/app/(app)/chores.tsx"
      provides: "Primary chores screen with personal-first split layout."
      contains: "My chores today"
    - path: "src/components/chores/CompleteChoreSheet.tsx"
      provides: "Completion confirmation flow with optional note and optional photo proof."
      contains: "Optional photo proof"
    - path: "src/components/chores/ChoreEditorSheet.tsx"
      provides: "Create/edit flow for recurring responsibilities and bonus chores."
      contains: "responsibility"
  key_links:
    - from: "src/app/(app)/chores.tsx"
      to: "src/hooks/useChores.ts"
      via: "load, filter, create, claim, and complete actions"
      pattern: "useChores\\("
    - from: "src/components/chores/ChoreCard.tsx"
      to: "src/lib/condition.ts"
      via: "condition bar and label rendering"
      pattern: "getConditionState|getConditionProgress"
    - from: "src/components/chores/CompleteChoreSheet.tsx"
      to: "supabase/migrations/00004_chores_calendar.sql"
      via: "complete_chore_instance RPC and optional proof-photo metadata"
      pattern: "complete_chore_instance|photo"
---

<objective>
Build the core chores UX: personal-first list layout, create/edit flows, condition bars, completion sheet with optional proof, and list filters.

Purpose: This keeps the main chores flow focused on CHOR-01 through CHOR-06 and CHOR-09, leaving stats/energy/gamification for a smaller follow-up plan.
Output: Chores screen, card/list/filter/editor/completion components, and focused chores UI tests.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-chores-calendar/03-CONTEXT.md
@.planning/phases/03-chores-calendar/03-05-PLAN.md
@src/app/(app)/chores.tsx
@src/components/ui/Card.tsx
@src/components/ui/Button.tsx
@src/hooks/useChores.ts
@src/lib/condition.ts
@src/types/chores.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the chores stub with the shared household list and create/edit flow</name>
  <files>src/__tests__/chores-ui.test.ts, src/app/(app)/chores.tsx, src/components/chores/ChoreCard.tsx, src/components/chores/ChoreSection.tsx, src/components/chores/ChoreFiltersBar.tsx, src/components/chores/ChoreEditorSheet.tsx</files>
  <action>Replace the local-state Phase 1 chores stub with a household-backed screen satisfying D-01 through D-05. The top section title must be the exact string `My chores today`, followed by a second household section. Group urgent chores first per D-02, then by area and assignee where useful. Create `ChoreCard` to render exactly the default face fields from D-03: title, assignee name(s), area, estimated minutes, condition bar, and a text condition label. Create `ChoreFiltersBar` with concrete filters for assignee, area, status, and urgency for CHOR-06. Create `ChoreEditorSheet` for create/edit with title, description, estimated minutes, area, member assignments, `kind` (`responsibility` or `bonus`), and recurrence controls that reuse the shared recurrence helpers for daily, weekdays, weekly, biweekly, monthly, and custom intervals.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chores-ui.test.ts</automated>
  </verify>
  <done>The chores tab supports creating, assigning, filtering, and viewing recurring or bonus chores in the personal-first split layout from D-01 through D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Add the completion confirmation and bonus-chore claim flow</name>
  <files>src/app/(app)/chores.tsx, src/components/chores/CompleteChoreSheet.tsx, src/__tests__/chores-ui.test.ts</files>
  <action>Create `CompleteChoreSheet` so tapping complete opens a confirmation sheet with optional note, optional photo proof, and optional actual-minutes entry per D-06 and D-07; the everyday completion path must stay valid when no photo is attached. Bonus chores must expose a separate claim action before completion to satisfy CHOR-09. Extend `src/__tests__/chores-ui.test.ts` so it asserts the personal-first layout, filter controls, completion sheet rendering, and a no-photo completion path.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chores-ui.test.ts</automated>
  </verify>
  <done>Users can claim bonus chores and complete chores through a lightweight confirmation flow that does not require photo proof.</done>
</task>

</tasks>

<verification>
Run the chores UI test file and confirm `src/app/(app)/chores.tsx` now contains the shared household flow instead of the Phase 1 stub.
</verification>

<success_criteria>
The chores tab behaves like a real household chores feature before advanced stats and gamification are layered on.
</success_criteria>

<output>
After completion, create `.planning/phases/03-chores-calendar/03-02-SUMMARY.md`
</output>
