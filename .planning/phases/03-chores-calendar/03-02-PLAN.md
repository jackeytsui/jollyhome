---
phase: 03-chores-calendar
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/__tests__/chores-ui.test.ts
  - src/__tests__/chores-stats-energy.test.ts
  - src/app/(app)/chores.tsx
  - src/app/(app)/chores/[id].tsx
  - src/components/chores/ChoreCard.tsx
  - src/components/chores/ChoreSection.tsx
  - src/components/chores/ChoreFiltersBar.tsx
  - src/components/chores/ChoreEditorSheet.tsx
  - src/components/chores/CompleteChoreSheet.tsx
  - src/components/chores/FairnessSummaryCard.tsx
  - src/components/chores/MemberFairnessRow.tsx
  - src/components/chores/EnergyLevelCard.tsx
  - src/components/chores/GamificationCard.tsx
autonomous: true
requirements:
  - CHOR-01
  - CHOR-02
  - CHOR-03
  - CHOR-04
  - CHOR-05
  - CHOR-06
  - CHOR-07
  - CHOR-08
  - CHOR-09
  - CHOR-10
must_haves:
  truths:
    - "Users can create, assign, filter, and complete chores from a shared household screen."
    - "The chores screen opens with a personal section first and keeps whole-household visibility below it."
    - "Fairness, energy, completion history, and optional gamification are usable without cluttering every card."
  artifacts:
    - path: "src/app/(app)/chores.tsx"
      provides: "Primary chores screen with personal-first split layout."
      contains: "My chores today"
    - path: "src/components/chores/CompleteChoreSheet.tsx"
      provides: "Completion confirmation flow with optional note/photo."
      contains: "optional photo proof"
    - path: "src/app/(app)/chores/[id].tsx"
      provides: "Dedicated detail/stats surface for history and fairness."
      contains: "Completion History"
  key_links:
    - from: "src/app/(app)/chores.tsx"
      to: "src/hooks/useChores.ts"
      via: "load, filter, create, and complete actions"
      pattern: "useChores\\("
    - from: "src/components/chores/CompleteChoreSheet.tsx"
      to: "supabase/migrations/00004_chores_calendar.sql"
      via: "complete_chore_instance RPC and proof-photo metadata"
      pattern: "complete_chore_instance|photo"
    - from: "src/app/(app)/chores/[id].tsx"
      to: "src/lib/fairness.ts"
      via: "history and fairness breakdown"
      pattern: "buildFairnessStats|getRollingAverageMinutes"
---

<objective>
Build the chores product surface: personal-first list layout, create/edit flows, condition bars, completion sheet with optional proof, filters, fairness history, energy adaptation, and optional gamification controls.

Purpose: This plan delivers the user-facing chore experience per D-01 through D-08 and D-20 through D-22.
Output: Chores screen, detail/stats route, chore UI component set, and tests for chores flows.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-chores-calendar/03-CONTEXT.md
@.planning/phases/03-chores-calendar/03-VALIDATION.md
@.planning/phases/03-chores-calendar/03-01-PLAN.md
@src/app/(app)/chores.tsx
@src/components/ui/Card.tsx
@src/components/ui/Button.tsx
@src/hooks/useChores.ts
@src/lib/condition.ts
@src/lib/fairness.ts
@src/types/chores.ts

<interfaces>
Use these exports from Plan 01:
```typescript
export function useChores(): {
  chores: ChoreInstance[];
  fairness: ChoreFairnessStats[];
  createChore(input: CreateChoreTemplateInput): Promise<boolean>;
  completeChore(input: CompleteChoreInput): Promise<boolean>;
  claimBonusChore(instanceId: string): Promise<boolean>;
}
```

```typescript
export function getConditionState(...): 'green' | 'yellow' | 'red';
export function getConditionProgress(...): number;
export function buildFairnessStats(...): ChoreFairnessStats[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the Phase 1 chores stub with the shared household chore flow</name>
  <files>src/__tests__/chores-ui.test.ts, src/app/(app)/chores.tsx, src/components/chores/ChoreCard.tsx, src/components/chores/ChoreSection.tsx, src/components/chores/ChoreFiltersBar.tsx, src/components/chores/ChoreEditorSheet.tsx, src/components/chores/CompleteChoreSheet.tsx</files>
  <read_first>src/app/(app)/chores.tsx, src/components/ui/Card.tsx, src/components/ui/Button.tsx, src/hooks/useChores.ts, src/lib/condition.ts, src/types/chores.ts, .planning/phases/03-chores-calendar/03-CONTEXT.md</read_first>
  <action>Replace the local-state Phase 1 `src/app/(app)/chores.tsx` stub with a household-backed screen that satisfies D-01 through D-07. The top section title must be the exact string `My chores today`, followed by a second section for the broader household list. Group chores with urgent items first per D-02, then by area and assignee where useful. Create `ChoreCard` to render exactly these default fields on the card face: title, assignee name(s), area, estimated minutes, condition bar, and a text condition label; do not put last-completed metadata on the card per D-03. Create `ChoreFiltersBar` with concrete filters for assignee, area, status, and urgency to satisfy CHOR-06. Create `ChoreEditorSheet` for create/edit with fields title, description, estimated minutes, area, member assignments, `kind` (`responsibility` or `bonus`), and recurrence controls reusing the shared recurrence helpers for daily, weekdays, weekly, biweekly, monthly, and custom intervals. Create `CompleteChoreSheet` so tapping complete opens a confirmation sheet with optional note, optional photo proof, and optional actual-minutes entry per D-06 and D-07; the happy path must not require a photo. Bonus chores must expose a claim action distinct from completion to satisfy CHOR-09.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chores-ui.test.ts</automated>
  </verify>
  <acceptance_criteria>- `src/app/(app)/chores.tsx` contains the exact string `My chores today`.\n- `src/app/(app)/chores.tsx` imports `useChores` and `ChoreFiltersBar`.\n- `src/components/chores/ChoreCard.tsx` contains `estimatedMinutes`, `area`, and either `conditionLabel` or explicit `green`/`yellow`/`red` label rendering.\n- `src/components/chores/CompleteChoreSheet.tsx` contains the strings `Optional note` and `Optional photo proof`.\n- `src/components/chores/ChoreEditorSheet.tsx` contains the exact strings `responsibility` and `bonus`.\n- `src/__tests__/chores-ui.test.ts` contains assertions covering `My chores today`, filters, and completion sheet rendering.\n- The automated test command exits successfully.</acceptance_criteria>
  <done>Users can create, assign, filter, claim, and complete chores from a real household screen that follows the personal-first and medium-density card decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Add dedicated detail, fairness, energy, and gamification surfaces</name>
  <files>src/__tests__/chores-stats-energy.test.ts, src/app/(app)/chores/[id].tsx, src/components/chores/FairnessSummaryCard.tsx, src/components/chores/MemberFairnessRow.tsx, src/components/chores/EnergyLevelCard.tsx, src/components/chores/GamificationCard.tsx, src/app/(app)/chores.tsx</files>
  <read_first>src/app/(app)/chores.tsx, src/hooks/useChores.ts, src/lib/fairness.ts, src/types/chores.ts, .planning/phases/03-chores-calendar/03-CONTEXT.md, .planning/phases/03-chores-calendar/03-VALIDATION.md</read_first>
  <action>Create `src/app/(app)/chores/[id].tsx` as the dedicated detail/stats route required by D-08. It must show `Completion History`, member fairness stats, actual-duration averages, and per-member counts/minutes. Add `FairnessSummaryCard` and `MemberFairnessRow` to show light indicators on the main screen and deeper breakdowns on the detail route per D-08 and D-12. Add `EnergyLevelCard` with a daily selector for concrete values like `low`, `medium`, `high`; it must save the entry through `useChores`/`member_energy_entries` and reorder the visible personal list using the energy-aware ranking helper for CHOR-08. Add `GamificationCard` backed by `household_chore_settings` so gamification can be toggled fully on/off per D-20 through D-22; when off, points/streak/leaderboard UI must disappear rather than showing zeros. Wire navigation from a chore card to the detail route and keep the main experience light-touch.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chores-stats-energy.test.ts</automated>
  </verify>
  <acceptance_criteria>- `src/app/(app)/chores/[id].tsx` contains the exact strings `Completion History` and `Fairness`.\n- `src/components/chores/EnergyLevelCard.tsx` contains the strings `low`, `medium`, and `high`.\n- `src/components/chores/GamificationCard.tsx` contains the exact string `Gamification` and a boolean toggle prop or state for enable/disable behavior.\n- `src/components/chores/FairnessSummaryCard.tsx` references both task-count and minute-based metrics, such as `tasksCompleted` and `minutesContributed`.\n- `src/__tests__/chores-stats-energy.test.ts` contains assertions for energy-based prioritization and gamification-off behavior.\n- The automated test command exits successfully.</acceptance_criteria>
  <done>The chores experience includes dedicated stats/history, daily energy adaptation, and optional gamification that disappears cleanly when disabled.</done>
</task>

</tasks>

<verification>
Run the chores UI and stats test files. Read `src/app/(app)/chores.tsx` and confirm the personal-first split layout, filter controls, completion-sheet flow, and detail-route link all exist.
</verification>

<success_criteria>
The chores tab feels like a real shared-household feature: users can manage recurring or bonus chores, understand urgency at a glance, complete with optional proof, and inspect fairness/energy without cluttering the main list.
</success_criteria>

<output>
After completion, create `.planning/phases/03-chores-calendar/03-02-SUMMARY.md`
</output>
