---
phase: 03-chores-calendar
plan: "04"
type: execute
wave: 5
depends_on:
  - "03-06"
  - "03-07"
files_modified:
  - src/__tests__/chore-rotation.test.ts
  - src/__tests__/phase3-flows.test.ts
  - src/lib/choreRotation.ts
  - src/hooks/useChoreRotation.ts
  - src/components/chores/RotationSuggestionCard.tsx
  - src/components/chores/RotationReviewSheet.tsx
  - src/app/(app)/chores.tsx
  - src/app/(app)/(home)/index.tsx
  - src/app/(app)/calendar.tsx
autonomous: true
requirements:
  - AICH-01
  - AICH-02
  - AICH-03
  - AICH-04
  - AICH-05
  - CHOR-05
  - CHOR-07
  - CALD-05
  - CALD-06
must_haves:
  truths:
    - "AI rotation suggestions are explainable, adjustable, and recomputed from current reality instead of a brittle queue."
    - "Availability, attendance, member roster changes, fairness load, and learned durations all affect assignment suggestions."
    - "Home and domain surfaces stay in sync when chores, RSVP, attendance, or household composition change."
  artifacts:
    - path: "src/lib/choreRotation.ts"
      provides: "Stateless rotation scoring and rationale generation."
      contains: "export function scoreRotationSuggestions"
    - path: "src/hooks/useChoreRotation.ts"
      provides: "Rotation load, rebalance, and apply hook."
      contains: "export function useChoreRotation"
    - path: "src/components/chores/RotationReviewSheet.tsx"
      provides: "Manual-adjust-before-confirm suggestion UX."
      contains: "Why this assignment"
  key_links:
    - from: "src/hooks/useChoreRotation.ts"
      to: "src/lib/choreRotation.ts"
      via: "scores open chores against availability, roster, load, preferences, and learned duration"
      pattern: "scoreRotationSuggestions"
    - from: "src/hooks/useChoreRotation.ts"
      to: "src/hooks/useCalendar.ts|src/hooks/useAttendance.ts|src/hooks/useChores.ts"
      via: "recomputes on timeline, attendance, and member-roster changes"
      pattern: "useCalendar|useAttendance|useChores"
    - from: "src/app/(app)/(home)/index.tsx"
      to: "src/hooks/useChores.ts|src/hooks/useCalendar.ts"
      via: "home summaries show live chores/calendar state"
      pattern: "useChores|useCalendar"
---

<objective>
Finish Phase 3 with assistive AI rotation and cross-surface integration: stateless suggestion scoring, manual override review, rebalance on availability or household composition change, and synchronized home/calendar summaries.

Purpose: This closes the AI promise of the phase without violating D-09 through D-12, and it adds explicit planning for AICH-04 composition changes.
Output: Rotation engine, rotation hook/UI, synchronized home summaries, and integration tests.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-chores-calendar/03-CONTEXT.md
@.planning/phases/03-chores-calendar/03-RESEARCH.md
@.planning/phases/03-chores-calendar/03-06-PLAN.md
@.planning/phases/03-chores-calendar/03-07-PLAN.md
@src/app/(app)/chores.tsx
@src/app/(app)/calendar.tsx
@src/app/(app)/(home)/index.tsx
@src/lib/fairness.ts
@src/hooks/useChores.ts
@src/hooks/useCalendar.ts
@src/hooks/useAttendance.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the stateless chore-rotation scoring engine and hook</name>
  <files>src/__tests__/chore-rotation.test.ts, src/lib/choreRotation.ts, src/hooks/useChoreRotation.ts</files>
  <behavior>- Test 1: Suggestions prefer members with higher current availability and lower trailing load for AICH-01.\n- Test 2: The algorithm does not depend on a persisted queue position for AICH-02.\n- Test 3: Learned average actual minutes influence effort estimation for AICH-05.\n- Test 4: Changing attendance, calendar conflicts, or active household member roster produces a different ranking for AICH-04.\n- Test 5: Every suggestion includes at least one short rationale string mentioning availability, load, preference, duration, or roster rebalancing per D-09.</behavior>
  <action>Write `src/__tests__/chore-rotation.test.ts` first, then implement `src/lib/choreRotation.ts` with pure exported functions `scoreRotationSuggestions`, `rebalanceSuggestions`, and `buildRotationRationale`. The engine must be stateless per D-11 and AICH-02: it may read current open chores, attendance, calendar conflicts, trailing fairness stats, preference weights, active household roster, and learned average minutes, but it must not store or require a queue position. Include member-roster inputs such as joined, left, or inactive members, and ensure rebalancing recomputes when the active member set changes, not just when availability changes, to satisfy AICH-04. Implement `src/hooks/useChoreRotation.ts` to load current inputs from `useChores`, `useCalendar`, and `useAttendance`, plus the current active household member list available through existing chores/calendar household data, and expose `suggestions`, `refreshSuggestions`, and `applySuggestions` functions.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chore-rotation.test.ts</automated>
  </verify>
  <done>The rotation engine is deterministic, explainable, and recomputes from current household availability and composition instead of stored turn order.</done>
</task>

<task type="auto">
  <name>Task 2: Add review-and-apply rotation UX and wire Phase 3 summaries across home and calendar</name>
  <files>src/__tests__/phase3-flows.test.ts, src/components/chores/RotationSuggestionCard.tsx, src/components/chores/RotationReviewSheet.tsx, src/app/(app)/chores.tsx, src/app/(app)/(home)/index.tsx, src/app/(app)/calendar.tsx</files>
  <action>Create `RotationSuggestionCard` and `RotationReviewSheet` so the chores screen can show suggested assignments with short rationale bullets and allow manual override before confirm per D-09 and D-10. The review sheet must include an explanation header like `Why this assignment` and an explicit `Confirm assignments` action. Wire `src/app/(app)/chores.tsx` to open the review sheet, edit assignees manually, and apply suggestions back into the chore assignment state. Update `src/app/(app)/(home)/index.tsx` so the home screen surfaces real chores and calendar summaries instead of generic text, including urgent chores and upcoming events. Update `src/app/(app)/calendar.tsx` so attendance, RSVP, or active-member changes trigger rotation refreshes and visible timeline updates. Write `src/__tests__/phase3-flows.test.ts` covering one full path: complete a chore with photo proof omitted, update attendance, change the active household roster, refresh rotation suggestions, and confirm a manual override. Also cover a flow where RSVP or attendance changes alter the suggested assignee list.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chore-rotation.test.ts src/__tests__/phase3-flows.test.ts</automated>
  </verify>
  <done>AI rotation ships as an assistive review flow and home/calendar surfaces stay synchronized when attendance, RSVP, chores, or household composition changes.</done>
</task>

</tasks>

<verification>
Run the rotation and phase-flow tests together. Confirm the review sheet allows manual override before confirmation and the integration tests cover both attendance-change and roster-change rebalancing.
</verification>

<success_criteria>
Phase 3 closes with explainable AI rotation, stateless rebalancing on availability or roster changes, and synchronized summaries across chores, calendar, and home.
</success_criteria>

<output>
After completion, create `.planning/phases/03-chores-calendar/03-04-SUMMARY.md`
</output>
