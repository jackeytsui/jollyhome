---
phase: 03-chores-calendar
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - src/__tests__/chore-rotation.test.ts
  - src/__tests__/phase3-flows.test.ts
  - src/lib/choreRotation.ts
  - src/hooks/useChoreRotation.ts
  - src/components/chores/RotationSuggestionCard.tsx
  - src/components/chores/RotationReviewSheet.tsx
  - src/app/(app)/chores.tsx
  - src/app/(app)/(home)/index.tsx
  - src/app/(app)/calendar.tsx
autonomous: true
requirements:
  - AICH-01
  - AICH-02
  - AICH-03
  - AICH-04
  - AICH-05
  - CHOR-05
  - CHOR-06
  - CHOR-07
  - CALD-05
  - CALD-06
must_haves:
  truths:
    - "AI rotation suggestions are explainable, adjustable, and recomputed from current reality instead of a brittle queue."
    - "Availability, attendance, fairness load, and learned durations all affect assignment suggestions."
    - "Home and domain surfaces stay in sync when chores, RSVP, or attendance change."
  artifacts:
    - path: "src/lib/choreRotation.ts"
      provides: "Stateless rotation scoring and rationale generation."
      contains: "export function scoreRotationSuggestions"
    - path: "src/hooks/useChoreRotation.ts"
      provides: "Rotation load/rebalance/apply hook."
      contains: "export function useChoreRotation"
    - path: "src/components/chores/RotationReviewSheet.tsx"
      provides: "Manual-adjust-before-confirm suggestion UX."
      contains: "Why this assignment"
  key_links:
    - from: "src/hooks/useChoreRotation.ts"
      to: "src/lib/choreRotation.ts"
      via: "score open chores against availability/load/preferences"
      pattern: "scoreRotationSuggestions"
    - from: "src/app/(app)/chores.tsx"
      to: "src/components/chores/RotationReviewSheet.tsx"
      via: "open suggestions, override, confirm"
      pattern: "RotationReviewSheet"
    - from: "src/app/(app)/(home)/index.tsx"
      to: "src/hooks/useChores.ts|src/hooks/useCalendar.ts"
      via: "home summaries show new chores/calendar state"
      pattern: "useChores|useCalendar"
---

<objective>
Finish Phase 3 with assistive AI rotation and cross-surface integration: stateless suggestion scoring, manual override review, rebalancing on availability change, and updated home/calendar summaries that reflect chores/calendar state.

Purpose: This plan closes the AI promise of the phase without violating D-09 through D-12, and it verifies the whole phase with high-value flows.
Output: Rotation engine, rotation hook/UI, synchronized home summaries, and integration tests.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-chores-calendar/03-CONTEXT.md
@.planning/phases/03-chores-calendar/03-VALIDATION.md
@.planning/phases/03-chores-calendar/03-RESEARCH.md
@.planning/phases/03-chores-calendar/03-02-PLAN.md
@.planning/phases/03-chores-calendar/03-03-PLAN.md
@src/app/(app)/chores.tsx
@src/app/(app)/calendar.tsx
@src/app/(app)/(home)/index.tsx
@src/lib/fairness.ts
@src/hooks/useChores.ts
@src/hooks/useCalendar.ts
@src/hooks/useAttendance.ts

<interfaces>
Rotation inputs expected from earlier plans:
```typescript
type RotationSignal = {
  memberId: string;
  availabilityScore: number;
  trailingTaskCount: number;
  trailingMinutes: number;
  preferenceWeight: number;
  todayEnergy: 'low' | 'medium' | 'high' | null;
  averageActualMinutes: number | null;
};
```

Executor should keep explanations short and human-readable per D-09:
```typescript
type RotationSuggestion = {
  choreInstanceId: string;
  suggestedMemberId: string;
  score: number;
  rationale: string[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the stateless chore-rotation scoring engine and hook</name>
  <files>src/__tests__/chore-rotation.test.ts, src/lib/choreRotation.ts, src/hooks/useChoreRotation.ts</files>
  <read_first>src/hooks/useChores.ts, src/hooks/useCalendar.ts, src/hooks/useAttendance.ts, src/lib/fairness.ts, src/types/chores.ts, src/types/calendar.ts, .planning/phases/03-chores-calendar/03-CONTEXT.md, .planning/phases/03-chores-calendar/03-RESEARCH.md</read_first>
  <behavior>- Test 1: Suggestions prefer members with higher current availability and lower trailing load for AICH-01.\n- Test 2: The algorithm does not depend on a persisted queue position for AICH-02.\n- Test 3: Learned average actual minutes influence effort estimation for AICH-05.\n- Test 4: Changing attendance or calendar conflicts produces a different ranking for AICH-04.\n- Test 5: Every suggestion includes at least one short rationale string mentioning availability, load, preference, or duration per D-09.</behavior>
  <action>Write `src/__tests__/chore-rotation.test.ts` first, then implement `src/lib/choreRotation.ts` with pure exported functions `scoreRotationSuggestions`, `rebalanceSuggestions`, and `buildRotationRationale`. The engine must be stateless per D-11 and AICH-02: it may read current open chores, attendance, calendar conflicts, trailing fairness stats, preference weights, and learned average minutes, but it must not store or require a `next_assignee_position` queue. Weight the score with concrete signals: availability overlap, rolling task count, rolling minutes, preference weight, energy multiplier, and learned duration. Implement `src/hooks/useChoreRotation.ts` to load current inputs from `useChores`, `useCalendar`, and `useAttendance`, recompute suggestions whenever chores/attendance/calendar data changes, and expose `suggestions`, `refreshSuggestions`, and `applySuggestions` functions.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chore-rotation.test.ts</automated>
  </verify>
  <acceptance_criteria>- `src/lib/choreRotation.ts` contains the exact exports `scoreRotationSuggestions`, `rebalanceSuggestions`, and `buildRotationRationale`.\n- `src/lib/choreRotation.ts` does not contain the string `queue_position` or `next_assignee_position`.\n- `src/hooks/useChoreRotation.ts` imports from both `useCalendar` and `useAttendance`.\n- `src/__tests__/chore-rotation.test.ts` contains assertions for availability, load, learned duration, and rebalance behavior.\n- The automated test command exits successfully.</acceptance_criteria>
  <done>The rotation engine is deterministic, explainable, and driven entirely by current household signals instead of stored turn order.</done>
</task>

<task type="auto">
  <name>Task 2: Add review-and-apply rotation UX and wire Phase 3 summaries across home and calendar</name>
  <files>src/__tests__/phase3-flows.test.ts, src/components/chores/RotationSuggestionCard.tsx, src/components/chores/RotationReviewSheet.tsx, src/app/(app)/chores.tsx, src/app/(app)/(home)/index.tsx, src/app/(app)/calendar.tsx</files>
  <read_first>src/app/(app)/chores.tsx, src/app/(app)/(home)/index.tsx, src/app/(app)/calendar.tsx, src/hooks/useChoreRotation.ts, src/hooks/useChores.ts, src/hooks/useCalendar.ts, src/components/ui/Card.tsx, .planning/phases/03-chores-calendar/03-CONTEXT.md</read_first>
  <action>Create `RotationSuggestionCard` and `RotationReviewSheet` so the chores screen can show suggested assignments with short rationale bullets and allow manual override before confirm per D-09 and D-10. The review sheet must include an explanation header like `Why this assignment` and an explicit `Confirm assignments` action. Wire `src/app/(app)/chores.tsx` to open the review sheet, edit assignees manually, and apply suggestions back into the chore assignment state. Update `src/app/(app)/(home)/index.tsx` so the home screen surfaces real chores and calendar summaries instead of generic text; include an urgent-chores preview and an upcoming-events preview using the same hook data. Update `src/app/(app)/calendar.tsx` if needed so attendance or RSVP changes trigger rotation refreshes and visible timeline updates. Write `src/__tests__/phase3-flows.test.ts` covering one full path: mark a chore complete with optional proof omitted, update attendance, refresh rotation suggestions, and confirm a manual override. Also cover a flow where RSVP or attendance changes alter the suggested assignee list.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chore-rotation.test.ts src/__tests__/phase3-flows.test.ts</automated>
  </verify>
  <acceptance_criteria>- `src/components/chores/RotationReviewSheet.tsx` contains the exact strings `Why this assignment` and `Confirm assignments`.\n- `src/app/(app)/chores.tsx` imports `useChoreRotation` and `RotationReviewSheet`.\n- `src/app/(app)/(home)/index.tsx` imports either `useChores` or `useCalendar` and no longer describes chores/calendar as purely personal-only copy.\n- `src/__tests__/phase3-flows.test.ts` contains assertions for completion without photo proof, attendance update, suggestion refresh, and manual override apply.\n- The automated test command exits successfully.</acceptance_criteria>
  <done>AI rotation is shipped as an assistive review flow, and the home/domain surfaces reflect the same chore/calendar state with automated end-to-end coverage.</done>
</task>

</tasks>

<verification>
Run the rotation and phase-flow tests together. Read the chores screen and confirm the review sheet allows manual override before confirmation, then confirm the home screen now uses real chores/calendar data hooks.
</verification>

<success_criteria>
Phase 3 closes with explainable AI rotation, stateless rebalancing when availability changes, and synchronized summaries across chores, calendar, and home surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/03-chores-calendar/03-04-SUMMARY.md`
</output>
