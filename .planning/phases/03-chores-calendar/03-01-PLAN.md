---
phase: 03-chores-calendar
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/__tests__/chores-core.test.ts
  - src/__tests__/calendar-core.test.ts
  - src/__tests__/fairness-condition.test.ts
  - src/types/chores.ts
  - src/types/calendar.ts
  - src/lib/recurrence.ts
  - src/lib/condition.ts
  - src/lib/fairness.ts
  - src/lib/calendarProjection.ts
  - src/hooks/useChores.ts
  - src/hooks/useCalendar.ts
  - src/hooks/useAttendance.ts
  - supabase/migrations/00004_chores_calendar.sql
autonomous: true
requirements:
  - CHOR-01
  - CHOR-02
  - CHOR-03
  - CHOR-04
  - CHOR-07
  - CHOR-08
  - CHOR-09
  - AICH-05
  - CALD-01
  - CALD-02
  - CALD-04
  - CALD-06
must_haves:
  truths:
    - "Household chore and calendar data has persistent tables, RLS, and typed client contracts."
    - "Recurring chores and events can be expressed from one shared recurrence system without custom per-screen date math."
    - "Condition, fairness, attendance, and projection logic are covered by automated tests before UI plans build on them."
  artifacts:
    - path: "supabase/migrations/00004_chores_calendar.sql"
      provides: "Phase 3 schema, RLS, indexes, storage policies, and RPC helpers."
      contains: "CREATE TABLE public.chore_templates"
    - path: "src/types/chores.ts"
      provides: "Chore template/instance/completion/fairness contracts."
      contains: "export interface ChoreTemplate"
    - path: "src/types/calendar.ts"
      provides: "Calendar event, RSVP, attendance, and projection contracts."
      contains: "export interface HouseholdCalendarItem"
    - path: "src/lib/recurrence.ts"
      provides: "Shared RRULE helpers for chores and events."
      contains: "export function buildRecurrenceRule"
    - path: "src/lib/calendarProjection.ts"
      provides: "Unified calendar projection layer with future source types."
      contains: "sourceType: 'event' | 'chore' | 'attendance' | 'meal' | 'maintenance' | 'guest' | 'quiet-hours' | 'booking'"
  key_links:
    - from: "src/hooks/useChores.ts"
      to: "supabase/migrations/00004_chores_calendar.sql"
      via: "queries against chore_templates, chore_instances, chore_completions, member_energy_entries"
      pattern: "from\\('chore_"
    - from: "src/hooks/useCalendar.ts"
      to: "src/lib/calendarProjection.ts"
      via: "project database rows into HouseholdCalendarItem[]"
      pattern: "projectCalendarItems"
    - from: "src/lib/condition.ts"
      to: "src/__tests__/fairness-condition.test.ts"
      via: "elapsed-time urgency and energy scoring assertions"
      pattern: "getConditionState|rankChoresForEnergy"
---

<objective>
Create the shared scheduling foundation for Phase 3: dependencies, typed contracts, tested recurrence/condition/projection helpers, and the Supabase schema/RLS needed by chores, calendar, attendance, and later AI rotation.

Purpose: All later plans depend on one recurrence model and one household-scoped data model; this prevents chore/event drift and keeps fairness/history queryable.
Output: Installed libraries, Wave 0 tests, Phase 3 types/libs/hooks foundation, and `00004_chores_calendar.sql`.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-chores-calendar/03-CONTEXT.md
@.planning/phases/03-chores-calendar/03-RESEARCH.md
@.planning/phases/03-chores-calendar/03-VALIDATION.md
@.planning/phases/02-expense-tracking-receipt-ocr/02-01-SUMMARY.md
@.planning/phases/02-expense-tracking-receipt-ocr/02-05-SUMMARY.md
@src/hooks/useRecurring.ts
@src/hooks/useExpenses.ts
@src/lib/expenseMath.ts
@src/types/expenses.ts
@supabase/migrations/00003_expenses.sql

<interfaces>
From src/hooks/useExpenses.ts:
```typescript
export function useExpenses(): {
  expenses: ExpenseWithSplits[];
  loading: boolean;
  error: string | null;
  createExpense(input: CreateExpenseInput): Promise<void>;
  loadExpenses(): Promise<void>;
}
```

From src/hooks/useRecurring.ts:
```typescript
export function useRecurring(): {
  templates: RecurringExpenseTemplate[];
  loading: boolean;
  processOverdue(): Promise<void>;
  loadTemplates(): Promise<void>;
}
```

Pattern to preserve from the repo:
```typescript
const channel = supabase
  .channel(`household:${activeHouseholdId}:expenses`)
  .on('postgres_changes', {...}, () => loadExpenses())
  .subscribe();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 3 libraries and write the Wave 0 contracts/tests</name>
  <files>package.json, package-lock.json, src/__tests__/chores-core.test.ts, src/__tests__/calendar-core.test.ts, src/__tests__/fairness-condition.test.ts, src/types/chores.ts, src/types/calendar.ts</files>
  <read_first>.planning/phases/03-chores-calendar/03-CONTEXT.md, .planning/phases/03-chores-calendar/03-RESEARCH.md, .planning/phases/03-chores-calendar/03-VALIDATION.md, package.json, jest.config.ts, src/types/expenses.ts, src/__tests__/expenses.test.ts</read_first>
  <action>Install the exact libraries recommended in research: `react-native-big-calendar@4.19.0`, `rrule@2.8.1`, `date-fns@4.1.0`, `@date-fns/tz@1.4.1`, and add Expo-compatible `@react-native-community/datetimepicker` with `npx expo install @react-native-community/datetimepicker` per the 2026-03-22 research snapshot. Create `src/types/chores.ts` with exported interfaces for `ChoreTemplate`, `ChoreAssignment`, `ChoreInstance`, `ChoreCompletion`, `ChoreFairnessStats`, `MemberEnergyEntry`, and `ChorePreference`. Create `src/types/calendar.ts` with `CalendarEvent`, `EventRsvp`, `AttendanceStatus`, and `HouseholdCalendarItem` where `sourceType` is the exact union `'event' | 'chore' | 'attendance' | 'meal' | 'maintenance' | 'guest' | 'quiet-hours' | 'booking'` to satisfy CALD-04 future-source compatibility. Create Wave 0 test files named exactly `src/__tests__/chores-core.test.ts`, `src/__tests__/calendar-core.test.ts`, and `src/__tests__/fairness-condition.test.ts` with failing-or-pending coverage for RRULE serialization, cadence/condition state, fairness rollups, actual-duration learning, attendance day-boundary behavior, and projection output shape. The test descriptions must explicitly reference D-05, D-11, D-17, and D-22 where relevant.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chores-core.test.ts src/__tests__/calendar-core.test.ts src/__tests__/fairness-condition.test.ts</automated>
  </verify>
  <acceptance_criteria>- `package.json` contains the strings `"react-native-big-calendar"`, `"rrule"`, `"date-fns"`, `"@date-fns/tz"`, and `"@react-native-community/datetimepicker"`.\n- `src/types/chores.ts` contains `export interface ChoreTemplate` and `export interface ChoreFairnessStats`.\n- `src/types/calendar.ts` contains `export interface HouseholdCalendarItem` and the literal union members `quiet-hours` and `booking`.\n- `src/__tests__/chores-core.test.ts` contains `describe('recurrence helpers'` and `describe('condition helpers'`.\n- `src/__tests__/calendar-core.test.ts` contains `describe('calendar projection'` and `describe('attendance day boundaries'`.\n- `src/__tests__/fairness-condition.test.ts` contains `describe('fairness rollups'` and `describe('energy-adapted prioritization'`.</acceptance_criteria>
  <done>Dependencies are declared, type contracts exist, and the Wave 0 test scaffold exists for every core logic area listed in 03-VALIDATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Supabase schema, RLS, storage, and RPC helpers for chores and calendar</name>
  <files>supabase/migrations/00004_chores_calendar.sql</files>
  <read_first>.planning/phases/03-chores-calendar/03-CONTEXT.md, .planning/phases/03-chores-calendar/03-RESEARCH.md, supabase/migrations/00001_foundation.sql, supabase/migrations/00003_expenses.sql</read_first>
  <action>Create `supabase/migrations/00004_chores_calendar.sql` following the existing top-level-DDL pattern from `00001_foundation.sql` and `00003_expenses.sql`. Add tables for `chore_templates`, `chore_assignments`, `chore_instances`, `chore_completions`, `member_energy_entries`, `member_chore_preferences`, `household_chore_settings`, `calendar_events`, `event_rsvps`, and `member_attendance`. `chore_templates` must include `title`, `description`, `estimated_minutes`, `area`, `kind` (`responsibility` or `bonus` per D-20/D-22), `recurrence_rule`, `cadence_days`, `timezone`, and `is_archived`. `chore_instances` must track `scheduled_for`, `condition_due_at`, `status`, and support one or more assignments via `chore_assignments`. `chore_completions` must store `completed_by`, `completed_at`, `note`, `photo_path`, and `actual_minutes` so AICH-05 can learn durations. `calendar_events` must include title/date-time/location/description/recurrence, and `event_rsvps` must use a simple enum-like text check for `going`, `maybe`, `not_going` per D-18. `member_attendance` must be a dedicated table keyed by household/member/date with `status` values `home_tonight` and `away_tonight` per D-17. Create indexes on household/date/status fields used by filters. Enable RLS on all new tables with household-member scoped SELECT and creator/admin/member write rules. Add a storage bucket policy for optional chore proof photos. Add RPC helpers for `complete_chore_instance`, `claim_bonus_chore`, and `upsert_attendance_status`. Add the new tables to the `supabase_realtime` publication if the existing migration pattern requires it.</action>
  <verify>
    <automated>rg -n "CREATE TABLE public\\.(chore_templates|chore_instances|chore_completions|calendar_events|event_rsvps|member_attendance)|CREATE OR REPLACE FUNCTION public\\.(complete_chore_instance|claim_bonus_chore|upsert_attendance_status)|ENABLE ROW LEVEL SECURITY" supabase/migrations/00004_chores_calendar.sql</automated>
  </verify>
  <acceptance_criteria>- `supabase/migrations/00004_chores_calendar.sql` contains `CREATE TABLE public.chore_templates`, `CREATE TABLE public.chore_instances`, `CREATE TABLE public.chore_completions`, `CREATE TABLE public.calendar_events`, `CREATE TABLE public.event_rsvps`, and `CREATE TABLE public.member_attendance`.\n- The migration contains the exact check strings `CHECK (kind IN ('responsibility', 'bonus'))`, `CHECK (status IN ('home_tonight', 'away_tonight'))`, and `CHECK (status IN ('going', 'maybe', 'not_going'))`.\n- The migration contains `photo_path TEXT` in `chore_completions`.\n- The migration contains `CREATE OR REPLACE FUNCTION public.complete_chore_instance`, `CREATE OR REPLACE FUNCTION public.claim_bonus_chore`, and `CREATE OR REPLACE FUNCTION public.upsert_attendance_status`.\n- The migration contains at least one `ALTER TABLE public.chore_templates ENABLE ROW LEVEL SECURITY` line and equivalent RLS lines for the calendar tables.</acceptance_criteria>
  <done>The database can persist chores, attendance, events, RSVP state, proof metadata, and fairness inputs with household-scoped access control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement shared recurrence, condition, fairness, projection, and base data hooks</name>
  <files>src/lib/recurrence.ts, src/lib/condition.ts, src/lib/fairness.ts, src/lib/calendarProjection.ts, src/hooks/useChores.ts, src/hooks/useCalendar.ts, src/hooks/useAttendance.ts, src/__tests__/chores-core.test.ts, src/__tests__/calendar-core.test.ts, src/__tests__/fairness-condition.test.ts</files>
  <read_first>src/__tests__/chores-core.test.ts, src/__tests__/calendar-core.test.ts, src/__tests__/fairness-condition.test.ts, src/hooks/useExpenses.ts, src/hooks/useRecurring.ts, src/hooks/useMembers.ts, src/lib/expenseMath.ts, src/lib/supabase.ts, src/types/chores.ts, src/types/calendar.ts, supabase/migrations/00004_chores_calendar.sql</read_first>
  <behavior>- Test 1: RRULE helpers serialize and parse daily, weekday, weekly, biweekly, monthly, and custom interval schedules used by CHOR-03 and CALD-02.\n- Test 2: Condition helpers convert elapsed time versus cadence into `green`, `yellow`, or `red` plus a normalized fill percent per D-05.\n- Test 3: Fairness helpers calculate both task-count and estimated/actual-minute totals per member per D-12 and AICH-05.\n- Test 4: Energy helpers rank urgent low-effort chores higher when a member reports low energy for CHOR-08.\n- Test 5: Calendar projection returns `HouseholdCalendarItem[]` for events, chore moments, and attendance while preserving lighter visual weight for non-event items per D-14.</behavior>
  <action>Implement `src/lib/recurrence.ts` around `rrule` with exported helpers `buildRecurrenceRule`, `parseRecurrenceRule`, `getNextOccurrence`, and `expandOccurrencesInRange`, using timezone-safe boundaries through `@date-fns/tz` and `date-fns`. Implement `src/lib/condition.ts` with `getConditionState`, `getConditionProgress`, and `rankChoresForEnergy`; condition must be elapsed-time based, not deadline-based, per D-05. Implement `src/lib/fairness.ts` with `buildFairnessStats`, `getRollingAverageMinutes`, and `summarizeMemberLoad` so both task count and minutes are emitted per member per D-12. Implement `src/lib/calendarProjection.ts` with `projectCalendarItems` and `groupAgendaItemsByDay`, and ensure non-event sources default to `visualWeight: 'secondary'` while `sourceType: 'event'` stays `primary` per D-14. Create `useChores`, `useCalendar`, and `useAttendance` hooks using the repo's `useState` + `useCallback` + `useEffect` pattern, loading household-scoped data from Supabase and subscribing to a channel name shaped like `household:${activeHouseholdId}:chores-calendar` for realtime refreshes. `useChores` must expose create/update/filter/complete/claim and fairness refresh helpers; `useCalendar` must expose load/create/update RSVP reload helpers and return projected calendar items; `useAttendance` must expose loadAttendance and upsertAttendance for `home_tonight` / `away_tonight` toggles.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chores-core.test.ts src/__tests__/calendar-core.test.ts src/__tests__/fairness-condition.test.ts</automated>
  </verify>
  <acceptance_criteria>- `src/lib/recurrence.ts` contains the exact exports `buildRecurrenceRule`, `parseRecurrenceRule`, `getNextOccurrence`, and `expandOccurrencesInRange`.\n- `src/lib/condition.ts` contains `export function getConditionState` and returns string states `green`, `yellow`, and `red` somewhere in the file.\n- `src/lib/fairness.ts` contains `export function buildFairnessStats` and `export function getRollingAverageMinutes`.\n- `src/lib/calendarProjection.ts` contains the exact string `visualWeight: 'secondary'` and references source types `attendance` and `chore`.\n- `src/hooks/useChores.ts` contains `channel(\\`household:${activeHouseholdId}:chores-calendar\\`)` or the same literal channel name shape.\n- `src/hooks/useAttendance.ts` contains `home_tonight` and `away_tonight`.\n- The automated test command exits successfully.</acceptance_criteria>
  <done>Phase 3 has one tested scheduling/fairness core and three household-scoped hooks that later plans can consume directly.</done>
</task>

</tasks>

<verification>
Run the three foundation test files together. Confirm the migration contains all required tables/functions/policies and the new hook/lib files export the exact interfaces referenced by later plans.
</verification>

<success_criteria>
The project has all Phase 3 dependencies installed, a tested recurrence/condition/fairness/projection layer, and a Supabase schema that supports chores, events, RSVP, attendance, proof photos, and future AI rotation inputs without redesign.
</success_criteria>

<output>
After completion, create `.planning/phases/03-chores-calendar/03-01-SUMMARY.md`
</output>
