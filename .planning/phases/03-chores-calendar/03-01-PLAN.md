---
phase: 03-chores-calendar
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/__tests__/chores-core.test.ts
  - src/__tests__/calendar-core.test.ts
  - src/__tests__/fairness-condition.test.ts
  - src/types/chores.ts
  - src/types/calendar.ts
autonomous: true
requirements:
  - CHOR-03
  - CHOR-04
  - CHOR-07
  - CHOR-08
  - AICH-05
  - CALD-02
  - CALD-04
  - CALD-06
must_haves:
  truths:
    - "Household scheduling data follows one shared contract for recurring chores, calendar items, attendance, and projected timeline entries before UI implementation begins."
    - "The calendar foundation can represent current Phase 3 items and later household activity types through one stable timeline shape instead of incompatible per-screen payloads."
    - "Phase 3 starts with passing executable scaffolds for condition, fairness, attendance, and projection behavior so later plans can add real logic without rewriting the public behavior surface."
  artifacts:
    - path: "src/types/chores.ts"
      provides: "Chore template, assignment, completion, fairness, energy, and preference contracts."
      contains: "export interface ChoreTemplate"
    - path: "src/types/calendar.ts"
      provides: "Calendar event, RSVP, attendance, and household timeline contracts."
      contains: "export interface HouseholdCalendarItem"
    - path: "src/__tests__/calendar-core.test.ts"
      provides: "Passing scaffold coverage for recurrence, attendance, and projection behavior."
      contains: "describe('calendar projection'"
  key_links:
    - from: "src/__tests__/chores-core.test.ts"
      to: "src/types/chores.ts"
      via: "typed scaffold cases for recurrence and completion data"
      pattern: "ChoreTemplate|ChoreCompletion"
    - from: "src/__tests__/calendar-core.test.ts"
      to: "src/types/calendar.ts"
      via: "projection and attendance contract references"
      pattern: "HouseholdCalendarItem|AttendanceStatus"
    - from: "package.json"
      to: "src/__tests__/calendar-core.test.ts"
      via: "calendar dependency installation for later implementation"
      pattern: "react-native-big-calendar|rrule|date-fns"
---

<objective>
Create the narrow Phase 3 foundation: install the agreed scheduling libraries, define stable chore/calendar contracts, and add passing scaffold tests that lock the public behavior shape without forcing implementation too early.

Purpose: This keeps the first plan small, fixes the verification contradiction, and gives downstream plans stable interfaces to build against.
Output: Installed dependencies, `src/types/chores.ts`, `src/types/calendar.ts`, and passing scaffold test files for chores/calendar core logic.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-chores-calendar/03-CONTEXT.md
@.planning/phases/03-chores-calendar/03-RESEARCH.md
@.planning/phases/03-chores-calendar/03-VALIDATION.md
@src/types/expenses.ts
@src/__tests__/expenses.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the Phase 3 scheduling dependencies and write passing scaffold tests</name>
  <files>package.json, package-lock.json, src/__tests__/chores-core.test.ts, src/__tests__/calendar-core.test.ts, src/__tests__/fairness-condition.test.ts</files>
  <action>Install the exact research-backed libraries `react-native-big-calendar@4.19.0`, `rrule@2.8.1`, `date-fns@4.1.0`, `@date-fns/tz@1.4.1`, and `@react-native-community/datetimepicker` per the 2026-03-22 Phase 3 research snapshot. Create the three test files as passing scaffold suites that use `describe`, `it.todo`, or other passing placeholders only; do not introduce failing assertions in this plan. The scaffold descriptions must explicitly reserve cases for D-05 condition colors, D-11 stateless rebalancing, D-17 attendance day boundaries, D-22 gamification-off behavior, and AICH-05 learned durations so later plans can replace the todos with real assertions.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chores-core.test.ts src/__tests__/calendar-core.test.ts src/__tests__/fairness-condition.test.ts</automated>
  </verify>
  <done>Dependencies are declared and the three Phase 3 core test files run successfully as scaffold suites with no failing assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Define the shared chore and calendar contracts</name>
  <files>src/types/chores.ts, src/types/calendar.ts</files>
  <action>Create `src/types/chores.ts` with exported interfaces for `ChoreTemplate`, `ChoreAssignment`, `ChoreInstance`, `ChoreCompletion`, `ChoreFairnessStats`, `MemberEnergyEntry`, and `ChorePreference`. Create `src/types/calendar.ts` with `CalendarEvent`, `EventRsvp`, `AttendanceStatus`, and `HouseholdCalendarItem`. Keep `HouseholdCalendarItem.sourceType` as the exact union `'event' | 'chore' | 'attendance' | 'meal' | 'maintenance' | 'guest' | 'quiet-hours' | 'booking'` so CALD-04 future timeline sources stay explicit from the start. Include fields for `iconKey`, `visualWeight`, recurrence metadata, and member color ownership so downstream calendar plans can implement CALD-03 and CALD-07 without revisiting these types.</action>
  <verify>
    <automated>rg -n "export interface (ChoreTemplate|ChoreFairnessStats|HouseholdCalendarItem|AttendanceStatus)|sourceType: 'event' \\| 'chore' \\| 'attendance' \\| 'meal' \\| 'maintenance' \\| 'guest' \\| 'quiet-hours' \\| 'booking'|iconKey|visualWeight" src/types/chores.ts src/types/calendar.ts</automated>
  </verify>
  <done>The Phase 3 domain contracts exist and downstream plans can consume them without guessing field names or unions.</done>
</task>

</tasks>

<verification>
Run the three scaffold test files together and confirm both type files export the exact interfaces and unions the later plans reference.
</verification>

<success_criteria>
Phase 3 begins with stable contracts, installed libraries, and passing scaffolds instead of a single overloaded foundation plan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-chores-calendar/03-01-SUMMARY.md`
</output>
