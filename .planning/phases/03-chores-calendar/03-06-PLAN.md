---
phase: 03-chores-calendar
plan: "06"
type: execute
wave: 4
depends_on:
  - "03-02"
files_modified:
  - src/__tests__/chores-stats-energy.test.ts
  - src/app/(app)/chores/[id].tsx
  - src/components/chores/FairnessSummaryCard.tsx
  - src/components/chores/MemberFairnessRow.tsx
  - src/components/chores/EnergyLevelCard.tsx
  - src/components/chores/GamificationCard.tsx
  - src/app/(app)/chores.tsx
autonomous: true
requirements:
  - CHOR-07
  - CHOR-08
  - CHOR-10
must_haves:
  truths:
    - "Users can inspect completion history and fairness without overcrowding every chore card."
    - "A member can set daily energy and see their visible chore ordering adapt."
    - "Gamification can be fully enabled or fully hidden without breaking core chores."
  artifacts:
    - path: "src/app/(app)/chores/[id].tsx"
      provides: "Dedicated detail/stats surface for history and fairness."
      contains: "Completion History"
    - path: "src/components/chores/EnergyLevelCard.tsx"
      provides: "Daily energy selector for low, medium, and high states."
      contains: "low"
    - path: "src/components/chores/GamificationCard.tsx"
      provides: "Optional light-touch points/streaks/leaderboard toggle surface."
      contains: "Gamification"
  key_links:
    - from: "src/app/(app)/chores/[id].tsx"
      to: "src/lib/fairness.ts"
      via: "member fairness and duration rollups"
      pattern: "buildFairnessStats|getRollingAverageMinutes"
    - from: "src/components/chores/EnergyLevelCard.tsx"
      to: "src/lib/condition.ts"
      via: "energy-aware ranking for personal list ordering"
      pattern: "rankChoresForEnergy"
    - from: "src/components/chores/GamificationCard.tsx"
      to: "supabase/migrations/00004_chores_calendar.sql"
      via: "household_chore_settings toggle"
      pattern: "household_chore_settings"
---

<objective>
Add the deeper chores surfaces: completion history, fairness breakdowns, daily energy adaptation, and optional gamification controls.

Purpose: This pulls the advanced chores work out of `03-02` so the main list flow stays implementable and the optional layers have their own acceptance criteria.
Output: Chore detail route, fairness/energy/gamification components, and dedicated tests for those behaviors.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-chores-calendar/03-CONTEXT.md
@.planning/phases/03-chores-calendar/03-02-PLAN.md
@src/app/(app)/chores.tsx
@src/hooks/useChores.ts
@src/lib/fairness.ts
@src/types/chores.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated chore detail and fairness surfaces</name>
  <files>src/__tests__/chores-stats-energy.test.ts, src/app/(app)/chores/[id].tsx, src/components/chores/FairnessSummaryCard.tsx, src/components/chores/MemberFairnessRow.tsx, src/app/(app)/chores.tsx</files>
  <action>Create `src/app/(app)/chores/[id].tsx` as the dedicated detail/stats route required by D-08. It must show `Completion History`, member fairness stats, actual-duration averages, and per-member counts/minutes. Add `FairnessSummaryCard` and `MemberFairnessRow` so the main screen surfaces light indicators while the detail route provides the deeper breakdown. Wire navigation from chore cards to the detail route and extend `src/__tests__/chores-stats-energy.test.ts` with assertions for history and both task-count and minute-based fairness metrics.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chores-stats-energy.test.ts</automated>
  </verify>
  <done>Users can inspect completion history and household fairness from a dedicated screen instead of overloaded chore cards.</done>
</task>

<task type="auto">
  <name>Task 2: Add energy adaptation and optional gamification controls</name>
  <files>src/components/chores/EnergyLevelCard.tsx, src/components/chores/GamificationCard.tsx, src/app/(app)/chores.tsx, src/__tests__/chores-stats-energy.test.ts</files>
  <action>Create `EnergyLevelCard` with concrete daily values `low`, `medium`, and `high`; persist entries through the existing chores data layer and use the energy-aware ranking helper so the visible personal list adapts for CHOR-08. Create `GamificationCard` backed by `household_chore_settings` so gamification can be toggled fully on or fully off per D-20 through D-22. When enabled, limit gamification to encouragement-oriented `points`, `streaks`, and a lightweight `leaderboard`; do not introduce penalties, late-fee mechanics, shame labels, or any punitive copy or scoring. When disabled, points, streaks, and leaderboard UI must disappear instead of showing zeroed placeholders. Extend `src/__tests__/chores-stats-energy.test.ts` with assertions for energy-based prioritization, gamification-off behavior, and the absence of punitive labels such as `Penalty`, `Late fee`, or `Missed-task punishment`.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chores-stats-energy.test.ts</automated>
  </verify>
  <done>The chores experience supports energy-aware prioritization and optional light-touch gamification without making those surfaces mandatory or punitive.</done>
</task>

</tasks>

<verification>
Run the chores stats/energy test file and confirm the detail route, fairness summaries, energy selector, and hidden-when-off gamification behavior all exist in source.
</verification>

<success_criteria>
Users can go deeper on fairness and energy when they need to, while the core chores flow stays clean when gamification is disabled.
</success_criteria>

<output>
After completion, create `.planning/phases/03-chores-calendar/03-06-SUMMARY.md`
</output>
