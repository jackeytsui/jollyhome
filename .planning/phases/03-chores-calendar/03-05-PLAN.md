---
phase: 03-chores-calendar
plan: "05"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - supabase/migrations/00004_chores_calendar.sql
  - src/lib/recurrence.ts
  - src/lib/condition.ts
  - src/lib/fairness.ts
  - src/lib/calendarProjection.ts
  - src/hooks/useChores.ts
  - src/hooks/useCalendar.ts
  - src/hooks/useAttendance.ts
autonomous: true
requirements:
  - CHOR-01
  - CHOR-02
  - CHOR-03
  - CHOR-04
  - CHOR-05
  - CHOR-07
  - CHOR-08
  - CHOR-09
  - AICH-05
  - CALD-01
  - CALD-02
  - CALD-04
  - CALD-05
  - CALD-06
must_haves:
  truths:
    - "Chores, attendance, RSVP, household events, and lightweight timeline entries for meals, maintenance, guests, quiet hours, and bookings can persist with household-scoped access rules."
    - "One shared recurrence, condition, fairness, and projection layer exists for chores plus every Phase 3 calendar activity type."
    - "The base hooks expose realtime household data instead of leaving each screen to hand-roll fetching and mutation logic."
  artifacts:
    - path: "supabase/migrations/00004_chores_calendar.sql"
      provides: "Phase 3 schema, RLS, indexes, storage policies, and RPC helpers."
      contains: "CREATE TABLE public.chore_templates"
    - path: "src/lib/calendarProjection.ts"
      provides: "Unified household timeline projection with event/chore/attendance plus meal/maintenance/guest/quiet-hours/booking support."
      contains: "projectCalendarItems"
    - path: "src/hooks/useAttendance.ts"
      provides: "Attendance load and upsert hook for home-tonight toggles."
      contains: "upsertAttendance"
  key_links:
    - from: "src/hooks/useChores.ts"
      to: "supabase/migrations/00004_chores_calendar.sql"
      via: "queries against chore tables and completion RPCs"
      pattern: "from\\('chore_|complete_chore_instance|claim_bonus_chore"
    - from: "src/hooks/useCalendar.ts"
      to: "src/lib/calendarProjection.ts"
      via: "projects event, chore, and attendance rows into timeline items"
      pattern: "projectCalendarItems"
    - from: "src/hooks/useAttendance.ts"
      to: "supabase/migrations/00004_chores_calendar.sql"
      via: "member attendance upsert RPC"
      pattern: "upsert_attendance_status"
---

<objective>
Create the real Phase 3 data foundation: schema, shared scheduling/fairness libs, and household-scoped hooks that every chores, calendar, and AI plan can reuse.

Purpose: This pulls the heavy schema/lib/hook work out of `03-01` so the foundation is implementable without overloading one plan.
Output: `00004_chores_calendar.sql`, shared libs for recurrence/condition/fairness/projection, and base `useChores`, `useCalendar`, `useAttendance` hooks.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-chores-calendar/03-CONTEXT.md
@.planning/phases/03-chores-calendar/03-RESEARCH.md
@.planning/phases/03-chores-calendar/03-01-PLAN.md
@src/hooks/useExpenses.ts
@src/hooks/useRecurring.ts
@src/lib/expenseMath.ts
@supabase/migrations/00001_foundation.sql
@supabase/migrations/00003_expenses.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 3 schema, RLS, storage, and RPC helpers</name>
  <files>supabase/migrations/00004_chores_calendar.sql</files>
  <action>Create `supabase/migrations/00004_chores_calendar.sql` following the existing top-level-DDL pattern. Add tables for `chore_templates`, `chore_assignments`, `chore_instances`, `chore_completions`, `member_energy_entries`, `member_chore_preferences`, `household_chore_settings`, `calendar_events`, `event_rsvps`, and `member_attendance`. `calendar_events` must include an `activity_type` field with the exact allowed values `event`, `meal`, `maintenance`, `guest`, `quiet_hours`, and `booking` so CALD-04 can render all required household activity types in Phase 3, even before later phases add richer domain logic. Respect D-17, D-18, D-20, and D-22 with explicit checks for attendance status, RSVP status, chore kind, and calendar activity type. Include `photo_path` and `actual_minutes` on `chore_completions` for CHOR-05 and AICH-05. Add indexes on household/date/status fields, household-scoped RLS policies, optional chore-photo storage policies, and RPC helpers `complete_chore_instance`, `claim_bonus_chore`, and `upsert_attendance_status`.</action>
  <verify>
    <automated>rg -n "CREATE TABLE public\\.(chore_templates|chore_instances|chore_completions|calendar_events|event_rsvps|member_attendance)|CHECK \\(kind IN \\('responsibility', 'bonus'\\)\\)|CHECK \\(status IN \\('home_tonight', 'away_tonight'\\)\\)|CHECK \\(status IN \\('going', 'maybe', 'not_going'\\)\\)|CHECK \\(activity_type IN \\('event', 'meal', 'maintenance', 'guest', 'quiet_hours', 'booking'\\)\\)|CREATE OR REPLACE FUNCTION public\\.(complete_chore_instance|claim_bonus_chore|upsert_attendance_status)|ENABLE ROW LEVEL SECURITY" supabase/migrations/00004_chores_calendar.sql</automated>
  </verify>
  <done>The database can persist chores, events, lightweight CALD-04 activity entries, RSVP, attendance, proof metadata, and fairness inputs with household-scoped access control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared libs and household hooks behind the contracts</name>
  <files>src/lib/recurrence.ts, src/lib/condition.ts, src/lib/fairness.ts, src/lib/calendarProjection.ts, src/hooks/useChores.ts, src/hooks/useCalendar.ts, src/hooks/useAttendance.ts</files>
  <behavior>- Test scaffolds from `03-01` now gain real implementations for RRULE expansion, elapsed-time condition bars, fairness rollups, and unified projection across chores, attendance, and all CALD-04 calendar activity types.\n- Hook contracts expose create/load/update helpers for chores, events, lightweight activity entries, RSVP, and attendance using household-scoped realtime refreshes.\n- Projection preserves stronger weight for events and lighter weight for chores/attendance while still assigning icons and grouping rules for meal, maintenance, guest, quiet-hours, and booking items.</behavior>
  <action>Implement `src/lib/recurrence.ts` with `buildRecurrenceRule`, `parseRecurrenceRule`, `getNextOccurrence`, and `expandOccurrencesInRange` using `rrule`, `date-fns`, and `@date-fns/tz`. Implement `src/lib/condition.ts` with `getConditionState`, `getConditionProgress`, and `rankChoresForEnergy` using elapsed time rather than deadline logic per D-05. Implement `src/lib/fairness.ts` with `buildFairnessStats`, `getRollingAverageMinutes`, and `summarizeMemberLoad` so both task count and minutes remain available per D-12 and AICH-05. Implement `src/lib/calendarProjection.ts` with `projectCalendarItems` and `groupAgendaItemsByDay`, mapping `calendar_events.activity_type` values into the exact `sourceType` values `event`, `meal`, `maintenance`, `guest`, `quiet-hours`, and `booking`, plus projected `chore` and `attendance` items; default non-event items to `visualWeight: 'secondary'`. Implement `useChores`, `useCalendar`, and `useAttendance` with the repo’s `useState` + `useCallback` + `useEffect` pattern, using one household realtime channel shaped like `household:${activeHouseholdId}:chores-calendar`.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/chores-core.test.ts src/__tests__/calendar-core.test.ts src/__tests__/fairness-condition.test.ts</automated>
  </verify>
  <done>The shared scheduling core and household data hooks can project every Phase 3 calendar activity type through one timeline model without changing public contracts later.</done>
</task>

</tasks>

<verification>
Run the three core test files and confirm the migration, libs, and hooks export the exact functions later plans expect.
</verification>

<success_criteria>
Phase 3 has one reusable persistence and scheduling core that chores, calendar, and AI flows can build on directly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-chores-calendar/03-05-SUMMARY.md`
</output>
