---
phase: 03-chores-calendar
plan: "07"
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - src/__tests__/calendar-projection.test.ts
  - src/app/(app)/calendar.tsx
  - src/components/calendar/CalendarViewSwitcher.tsx
  - src/components/calendar/CalendarAgendaList.tsx
autonomous: true
requirements:
  - CALD-03
  - CALD-04
  - CALD-07
must_haves:
  truths:
    - "Users can browse the household timeline in day, week, month, and agenda views."
    - "Week remains the default power view while agenda stays available for mobile scanning."
    - "Projected items preserve event prominence while still showing chore, attendance, meal, maintenance, guest, quiet-hours, and booking icons/colors in the rendered calendar."
  artifacts:
    - path: "src/app/(app)/calendar.tsx"
      provides: "Primary household calendar screen with all required views."
      contains: "Day"
    - path: "src/components/calendar/CalendarViewSwitcher.tsx"
      provides: "Explicit view controls for day, week, month, and agenda."
      contains: "Agenda"
    - path: "src/components/calendar/CalendarAgendaList.tsx"
      provides: "Agenda/mobile scan view over projected calendar items."
      contains: "Agenda"
  key_links:
    - from: "src/app/(app)/calendar.tsx"
      to: "react-native-big-calendar"
      via: "day, week, and month rendering"
      pattern: "from 'react-native-big-calendar'"
    - from: "src/app/(app)/calendar.tsx"
      to: "src/lib/calendarProjection.ts"
      via: "renders projected event, chore, and attendance items"
      pattern: "projectCalendarItems"
    - from: "src/app/(app)/calendar.tsx"
      to: "src/components/calendar/CalendarAgendaList.tsx"
      via: "agenda fallback rendering"
      pattern: "CalendarAgendaList"
---

<objective>
Replace the calendar stub with the full rendered household timeline, including all required views and projected icon/color cues for every CALD-04 activity type.

Purpose: This isolates the heavier rendering work and makes day/week/month/agenda support explicit for CALD-07.
Output: Real calendar screen, view switcher, agenda list, and projection/rendering tests.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-chores-calendar/03-CONTEXT.md
@.planning/phases/03-chores-calendar/03-03-PLAN.md
@src/app/(app)/calendar.tsx
@src/hooks/useCalendar.ts
@src/lib/calendarProjection.ts
@src/types/calendar.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the calendar stub with day/week/month/agenda household rendering</name>
  <files>src/__tests__/calendar-projection.test.ts, src/app/(app)/calendar.tsx, src/components/calendar/CalendarViewSwitcher.tsx, src/components/calendar/CalendarAgendaList.tsx</files>
  <action>Replace the current `src/app/(app)/calendar.tsx` stub with a full screen built around `react-native-big-calendar`. Week view must be the default selected mode per D-13, but the screen must also provide explicit `Day`, `Week`, `Month`, and `Agenda` options to satisfy CALD-07 completely. Create `CalendarViewSwitcher` with those four exact options. Use the projection layer so events render with `visualWeight: 'primary'`, chores and attendance render lighter per D-14 and D-16, and projected items display the event-type icon mapping defined in `03-03` for `event`, `chore`, `attendance`, `meal`, `maintenance`, `guest`, `quiet-hours`, and `booking`. Render all CALD-04 activity types in the same calendar and agenda views, even when some are represented as lightweight Phase 3 calendar entries rather than later-phase specialized workflows. Add `CalendarAgendaList` as the mobile-scannable agenda rendering path.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/calendar-ui.test.ts src/__tests__/calendar-projection.test.ts</automated>
  </verify>
  <done>The calendar tab is a rendered household timeline with explicit day/week/month/agenda support, event-type icons, agenda scanability, and all CALD-04 activity types visible in one view.</done>
</task>

</tasks>

<verification>
Run the calendar UI and projection test files together and confirm `src/app/(app)/calendar.tsx` includes all four view options plus agenda rendering.
</verification>

<success_criteria>
Users can treat calendar as a real shared timeline with all required view modes and legible projected item rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/03-chores-calendar/03-07-SUMMARY.md`
</output>
