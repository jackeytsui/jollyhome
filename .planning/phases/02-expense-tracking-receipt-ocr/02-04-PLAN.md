---
phase: 02-expense-tracking-receipt-ocr
plan: 04
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/components/expenses/ExpenseDetailSheet.tsx
  - src/components/expenses/ChangeHistoryRow.tsx
  - src/components/expenses/FilterBar.tsx
  - src/components/expenses/DisputeBadge.tsx
  - src/app/(app)/expense-history.tsx
autonomous: true
requirements: [EXPN-05, EXPN-10, EXPN-12, EXPN-14]

must_haves:
  truths:
    - "User can view expense history with filters (date, category, member, amount range)"
    - "User can edit or delete an expense with visible change history"
    - "User can dispute an expense with a note and discussion thread"
    - "User can set privacy tiers per expense (visible to all or involved only)"
    - "Deleted expenses show soft-delete marker in history"
  artifacts:
    - path: "src/components/expenses/ExpenseDetailSheet.tsx"
      provides: "Two-tab expense detail with Details and History tabs, edit/delete/dispute actions"
      exports: ["ExpenseDetailSheet"]
    - path: "src/components/expenses/FilterBar.tsx"
      provides: "Horizontal scrollable filter chips for expense history"
      exports: ["FilterBar"]
    - path: "src/app/(app)/expense-history.tsx"
      provides: "Full expense history screen with pagination and filters"
      min_lines: 50
  key_links:
    - from: "src/components/expenses/ExpenseDetailSheet.tsx"
      to: "supabase RPC update_expense/soft_delete_expense"
      via: "supabase.rpc calls"
      pattern: "rpc.*update_expense|rpc.*soft_delete_expense"
    - from: "src/components/expenses/FilterBar.tsx"
      to: "src/stores/expenses.ts"
      via: "useExpenseStore setFilters/clearFilters"
      pattern: "setFilters|clearFilters"
---

<objective>
Build expense history with filtering, expense detail view with edit/delete/dispute capabilities, change history audit trail, and privacy toggle.

Purpose: These are the power-user features that differentiate HomeOS from Splitwise: audit trails, disputes, and granular privacy. Essential for trust in shared financial tracking.

Output: ExpenseDetailSheet with two-tab detail view, FilterBar, full expense history screen, DisputeBadge, ChangeHistoryRow.
</objective>

<execution_context>
@/Users/jackeytsui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-expense-tracking-receipt-ocr/02-CONTEXT.md
@.planning/phases/02-expense-tracking-receipt-ocr/02-UI-SPEC.md
@.planning/phases/02-expense-tracking-receipt-ocr/02-01-SUMMARY.md
@.planning/phases/02-expense-tracking-receipt-ocr/02-02-SUMMARY.md

<interfaces>
From src/types/expenses.ts:
```typescript
export interface Expense { id: string; description: string; amount_cents: number; category: string | null; split_type: SplitType; is_private: boolean; deleted_at: string | null; deleted_by: string | null; ... }
export interface ExpenseVersion { id: string; expense_id: string; changed_by: string; change_type: 'created'|'edited'|'deleted'; previous_data: Record<string, unknown> | null; changed_fields: string[] | null; created_at: string; }
export interface ExpenseDispute { id: string; expense_id: string; opened_by: string; status: 'open'|'resolved'; ... }
export interface DisputeComment { id: string; dispute_id: string; user_id: string; body: string; created_at: string; }
export interface ExpenseFilters { dateFrom: string | null; dateTo: string | null; category: string | null; memberId: string | null; amountMin: number | null; amountMax: number | null; search: string | null; }
```

From src/hooks/useExpenses.ts (Plan 02):
```typescript
export function useExpenses(): { expenses: Expense[]; loading: boolean; createExpense: ...; loadExpenses: ...; }
```

From src/stores/expenses.ts (Plan 01):
```typescript
// activeFilters, setFilters, clearFilters
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExpenseDetailSheet with edit, delete, dispute, and change history</name>
  <files>src/components/expenses/ExpenseDetailSheet.tsx, src/components/expenses/ChangeHistoryRow.tsx, src/components/expenses/DisputeBadge.tsx</files>
  <read_first>
    - src/components/ui/Card.tsx (Card component)
    - src/components/ui/Button.tsx (Button component)
    - src/constants/theme.ts (colors)
    - src/hooks/useExpenses.ts (from Plan 02)
    - src/lib/supabase.ts (supabase client for RPC calls and dispute queries)
    - src/types/expenses.ts (ExpenseVersion, ExpenseDispute, DisputeComment types)
    - .planning/phases/02-expense-tracking-receipt-ocr/02-UI-SPEC.md (ExpenseDetailSheet layout, DisputeBadge spec, ChangeHistoryRow spec)
    - .planning/phases/02-expense-tracking-receipt-ocr/02-CONTEXT.md (edit with audit trail, soft delete, disputes, privacy)
  </read_first>
  <action>
    **`src/components/expenses/DisputeBadge.tsx`:**
    - Props: `{ status: 'open' | 'resolved' }`
    - Small pill: sandbox amber (#CA8A04) bg for open, success green for resolved. Text "Disputed" or "Resolved". Flag icon from lucide-react-native (Flag).
    - Height 20px, border radius 10px, horizontal padding 8px, font 14px weight 600

    **`src/components/expenses/ChangeHistoryRow.tsx`:**
    - Props: `{ version: ExpenseVersion; memberName: string }`
    - Row: small avatar (20px) + action text + timestamp (textSecondary)
    - Action text generated from change_type: "created" -> "{name} created this expense", "edited" -> "{name} edited {changed_fields.join(', ')}", "deleted" -> "{name} deleted this expense"
    - If previous_data exists, show expandable diff (old value -> new value) for each changed field
    - Timestamp formatted as relative ("2h ago", "Yesterday", date for older)

    **`src/components/expenses/ExpenseDetailSheet.tsx`:**
    - Props: `{ visible: boolean; onClose: () => void; expense: Expense & { expense_splits: ExpenseSplit[] }; onUpdate: () => void }`
    - Uses `@gorhom/bottom-sheet` BottomSheet (snap points: ['80%', '95%'])
    - Two-tab layout using a simple tab bar at top (Pressable row with underline indicator):

    **Tab 1: "Details"**
    - Amount in Display 28px, accent color
    - Description in Heading 20px
    - Category chip (if set)
    - Date
    - Paid by: avatar + name
    - Split breakdown: list of MemberSplitRow (read-only) showing each person's share
    - Receipt thumbnail (Image component) if receipt_url exists — tappable to view full-size
    - Privacy indicator: Lock icon + "Private" text if is_private=true
    - If expense has open dispute: DisputeBadge rendered below amount

    **Tab 2: "History"**
    - Fetch `expense_versions` WHERE expense_id, ordered by created_at DESC
    - Fetch `expense_disputes` WHERE expense_id with related `dispute_comments`
    - Display: ChangeHistoryRow for each version + dispute section below if disputes exist
    - Dispute section: each dispute shows opened_by avatar + note + comment thread (DisputeComment items)
    - "Add comment" TextInput at bottom of dispute thread

    **Action buttons at bottom:**
    - "Edit" Button: Opens inline editing mode — description, amount, category, split become editable TextInputs/pickers. Save calls `supabase.rpc('update_expense', { p_expense_id: expense.id, p_description, p_amount_cents, p_category, p_split_type, p_splits: JSON.stringify(newSplits), p_tax_cents, p_tip_cents, p_is_private })`. Haptic success on save. Calls onUpdate().
    - "Delete" Button (destructive variant): Shows confirmation Alert with copy "Delete this expense? This will update balances for everyone involved." with "Delete Expense" / "Keep It" buttons. On confirm: `supabase.rpc('soft_delete_expense', { p_expense_id: expense.id })`. Haptic success. Calls onUpdate() and onClose().
    - "Dispute" Button: Shows TextInput with placeholder "Add a note for your housemates...". Submit creates dispute: `supabase.from('expense_disputes').insert({ expense_id: expense.id, opened_by: currentUserId })` then `supabase.from('dispute_comments').insert({ dispute_id, user_id: currentUserId, body: note })`. Haptic notification.

    Only show Edit/Delete if current user is the expense creator. Dispute available to all involved members.
  </action>
  <verify>
    <automated>ls src/components/expenses/ExpenseDetailSheet.tsx src/components/expenses/ChangeHistoryRow.tsx src/components/expenses/DisputeBadge.tsx && grep -c "update_expense\|soft_delete_expense\|expense_disputes" src/components/expenses/ExpenseDetailSheet.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/expenses/ExpenseDetailSheet.tsx contains `export function ExpenseDetailSheet`
    - src/components/expenses/ExpenseDetailSheet.tsx contains `Details` and `History` (tab names)
    - src/components/expenses/ExpenseDetailSheet.tsx contains `rpc.*update_expense` or `update_expense`
    - src/components/expenses/ExpenseDetailSheet.tsx contains `rpc.*soft_delete_expense` or `soft_delete_expense`
    - src/components/expenses/ExpenseDetailSheet.tsx contains `expense_disputes`
    - src/components/expenses/ExpenseDetailSheet.tsx contains `Delete this expense`
    - src/components/expenses/ExpenseDetailSheet.tsx contains `dispute_comments`
    - src/components/expenses/ChangeHistoryRow.tsx contains `export function ChangeHistoryRow`
    - src/components/expenses/ChangeHistoryRow.tsx contains `change_type` or `changeType`
    - src/components/expenses/DisputeBadge.tsx contains `export function DisputeBadge`
    - src/components/expenses/DisputeBadge.tsx contains `Disputed`
  </acceptance_criteria>
  <done>ExpenseDetailSheet shows full expense details with edit/delete/dispute actions. Change history displays audit trail. Disputes open threaded discussions. Edit uses RPC for atomic update with version snapshot. Delete is soft with confirmation.</done>
</task>

<task type="auto">
  <name>Task 2: Expense history screen with filter bar and pagination</name>
  <files>src/components/expenses/FilterBar.tsx, src/app/(app)/expense-history.tsx</files>
  <read_first>
    - src/app/(app)/finances.tsx (current expenses tab — to wire "See all expenses" link)
    - src/stores/expenses.ts (activeFilters, setFilters, clearFilters)
    - src/hooks/useExpenses.ts (loadExpenses for filtered queries)
    - src/types/expenses.ts (ExpenseFilters)
    - src/constants/theme.ts (colors)
    - src/hooks/useMembers.ts (member list for member filter)
    - .planning/phases/02-expense-tracking-receipt-ocr/02-UI-SPEC.md (FilterBar spec)
    - .planning/phases/02-expense-tracking-receipt-ocr/02-CONTEXT.md (expense history section)
  </read_first>
  <action>
    **`src/components/expenses/FilterBar.tsx`:**
    - Props: `{ filters: ExpenseFilters; onFilterChange: (filters: Partial<ExpenseFilters>) => void; onClear: () => void; members: Member[] }`
    - Horizontal ScrollView of filter chip Pressables:
      1. "Date" chip: tapping opens inline date range picker (two TextInputs: From YYYY-MM-DD, To YYYY-MM-DD). Active when dateFrom or dateTo set.
      2. "Category" chip: tapping opens category list (same list as CategoryChipSuggestion). Active when category set.
      3. "Member" chip: tapping opens member list with avatars. Active when memberId set.
      4. "Amount" chip: tapping shows min/max TextInputs (decimal-pad). Active when amountMin or amountMax set.
      5. "Search" chip: toggles a TextInput search bar below filter row. Active when search set.
    - Active chip style: accent bg (#F97316) + white text
    - Inactive chip style: secondary bg (#F5EDE0) + textPrimary
    - "Clear" button at end of scroll (only visible when any filter active)
    - Chip height 32px, border radius 16px, horizontal padding 12px, gap 8px

    **`src/app/(app)/expense-history.tsx`:**
    - Full-screen Expo Router page (accessible from "See all expenses" link on finances.tsx)
    - SafeAreaView + FlatList layout (not ScrollView — needed for pagination)
    - Header: "Expense History" heading (Heading 20px)
    - FilterBar at top (sticky)
    - FlatList of ExpenseCard items, data from useExpenses with filters applied
    - Filtered query: extend useExpenses to accept `ExpenseFilters` parameter. Build Supabase query dynamically:
      - `.eq('category', filter.category)` when set
      - `.gte('expense_date', filter.dateFrom)` when set
      - `.lte('expense_date', filter.dateTo)` when set
      - `.gte('amount_cents', filter.amountMin)` when set
      - `.lte('amount_cents', filter.amountMax)` when set
      - `.ilike('description', `%${filter.search}%`)` when set
      - Member filter: join with expense_splits where user_id = memberId
    - Pagination: load 20 items at a time. `onEndReached` loads next page via `.range(offset, offset+19)`
    - Tapping any ExpenseCard opens ExpenseDetailSheet (from Task 1)
    - Empty state: "No expenses match your filters" with clear filters button
    - Wire finances.tsx "See all expenses" link: `router.push('/expense-history')`

    Also update `src/hooks/useExpenses.ts` to add a `loadFilteredExpenses(filters: ExpenseFilters, page: number)` function that builds the dynamic query.
  </action>
  <verify>
    <automated>ls src/components/expenses/FilterBar.tsx src/app/\(app\)/expense-history.tsx && grep -c "setFilters\|clearFilters\|ExpenseFilters" src/components/expenses/FilterBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/expenses/FilterBar.tsx contains `export function FilterBar`
    - src/components/expenses/FilterBar.tsx contains `Date` and `Category` and `Member` and `Amount`
    - src/components/expenses/FilterBar.tsx contains filter active state styling (accent color)
    - src/app/(app)/expense-history.tsx contains `FlatList`
    - src/app/(app)/expense-history.tsx contains `ExpenseCard`
    - src/app/(app)/expense-history.tsx contains `FilterBar`
    - src/app/(app)/expense-history.tsx contains `onEndReached` or pagination logic
    - src/app/(app)/expense-history.tsx contains `ExpenseDetailSheet`
    - src/hooks/useExpenses.ts contains `loadFilteredExpenses` or filter-aware query logic
  </acceptance_criteria>
  <done>Full expense history screen with 5 filter types (date, category, member, amount, search), paginated FlatList, and ExpenseDetailSheet integration. FilterBar uses accent chip styling for active filters.</done>
</task>

</tasks>

<verification>
ls src/components/expenses/FilterBar.tsx src/components/expenses/ExpenseDetailSheet.tsx src/components/expenses/ChangeHistoryRow.tsx src/components/expenses/DisputeBadge.tsx
ls src/app/\(app\)/expense-history.tsx
npx jest src/__tests__/expenses.test.ts --passWithNoTests
</verification>

<success_criteria>
Users can view expense history with 5 filter types, tap any expense to see full details with two-tab view (Details + History), edit expenses with audit trail, soft-delete with confirmation, open disputes with threaded discussion, and see privacy indicators.
</success_criteria>

<output>
After completion, create `.planning/phases/02-expense-tracking-receipt-ocr/02-04-SUMMARY.md`
</output>
