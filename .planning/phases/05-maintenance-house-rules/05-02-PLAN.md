---
phase: 05-maintenance-house-rules
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - supabase/migrations/00007_maintenance_house_rules.sql
  - src/types/rules.ts
  - src/hooks/useHouseRules.ts
  - src/app/(app)/rules.tsx
  - src/components/rules/RuleVersionCard.tsx
  - src/components/rules/RuleEditorSheet.tsx
  - src/components/rules/RuleAcknowledgementSheet.tsx
  - src/components/rules/CoordinationEventSheet.tsx
  - src/__tests__/rules-coordination-ui.test.ts
autonomous: true
requirements:
  - RULE-01
  - RULE-02
  - RULE-03
  - RULE-04
  - RULE-05
must_haves:
  truths:
    - "House rules are versioned, historically visible, and explicitly acknowledged per version by household members."
    - "Quiet hours, guest notices, and shared-space bookings show up in the shared calendar using the existing activity timeline."
    - "Rules and coordination objects stay lightweight household operations tools rather than turning into a generic docs or scheduling subsystem."
  artifacts:
    - path: "src/hooks/useHouseRules.ts"
      provides: "Rule versioning, acknowledgements, and coordination-event orchestration."
      contains: "acknowledgeCurrentVersion"
    - path: "src/app/(app)/rules.tsx"
      provides: "Rules screen with version history, current policy view, and coordination shortcuts."
      contains: "House Rules"
    - path: "src/components/rules/CoordinationEventSheet.tsx"
      provides: "Quick create/edit flow for quiet hours, guest notices, and shared-space bookings."
      contains: "quiet_hours"
  key_links:
    - from: "src/hooks/useHouseRules.ts"
      to: "src/hooks/useCalendar.ts"
      via: "quiet hours, guest notices, and bookings are persisted as calendar activities"
      pattern: "activity_type.*quiet_hours|activity_type.*guest|activity_type.*booking"
    - from: "src/app/(app)/rules.tsx"
      to: "src/hooks/useHouseRules.ts"
      via: "rules screen remains hook-driven and history-aware"
      pattern: "useHouseRules"
---

<objective>
Ship the rules and coordination layer: versioned house rules with acknowledgements, plus quiet hours, guest notices, and shared-space bookings projected into the household calendar.

Purpose: This plan covers the shared-living governance half of Phase 05 while reusing the existing calendar system for time-based coordination.
Output: A rules screen, rule-versioning data model, acknowledgement flow, and lightweight coordination event editors for quiet hours, guests, and bookings.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-maintenance-house-rules/05-CONTEXT.md
@.planning/phases/05-maintenance-house-rules/05-RESEARCH.md
@src/hooks/useCalendar.ts
@src/lib/calendarProjection.ts
@src/components/calendar/EventEditorSheet.tsx
@src/app/(app)/calendar.tsx

<interfaces>
New contract in src/types/rules.ts:
```typescript
export interface HouseRuleVersion {
  id: string;
  householdId: string;
  versionLabel: string;
  body: string;
  isCurrent: boolean;
  publishedAt: string;
}

export interface RuleAcknowledgement {
  ruleVersionId: string;
  memberId: string;
  acknowledgedAt: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build rules versioning, acknowledgements, and current-rules UX</name>
  <files>supabase/migrations/00007_maintenance_house_rules.sql, src/types/rules.ts, src/hooks/useHouseRules.ts, src/app/(app)/rules.tsx, src/components/rules/RuleVersionCard.tsx, src/components/rules/RuleEditorSheet.tsx, src/components/rules/RuleAcknowledgementSheet.tsx, src/__tests__/rules-coordination-ui.test.ts</files>
  <behavior>
    - Test 1: the household can create or update house rules and retain version history instead of overwriting history in place.
    - Test 2: members can acknowledge the current version, and the system records who accepted which version.
    - Test 3: the rules surface keeps prior versions visible so changes over time are inspectable.
  </behavior>
  <action>Replace the rules scaffold tests with real assertions. Add the house-rule version and acknowledgement schema to the shared Phase 05 migration. Implement `src/types/rules.ts` and `useHouseRules` so the current version, prior versions, and member acknowledgements load through one hook. Build the main rules surface and sheets for editing and acknowledging versions. Keep the rules body format simple and maintainable; plain formatted text or sectioned text is acceptable as long as versioning is explicit and history remains visible.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/rules-coordination-ui.test.ts</automated>
  </verify>
  <done>The household has a durable, versioned rules record with explicit member acknowledgements and visible history.</done>
</task>

<task type="auto">
  <name>Task 2: Add quiet hours, guest notices, and shared-space booking flows through the calendar layer</name>
  <files>src/hooks/useHouseRules.ts, src/components/rules/CoordinationEventSheet.tsx, src/app/(app)/rules.tsx, src/__tests__/rules-coordination-ui.test.ts</files>
  <action>Implement lightweight create/edit flows for quiet hours, guest notices, and shared-space bookings. Persist them into the existing calendar event system with the appropriate `activity_type` values (`quiet_hours`, `guest`, `booking`) so they appear in the shared timeline automatically. Reuse the existing event editor semantics where practical, but keep the Phase 05 flows faster and more opinionated than the general event editor. Include booking conflict visibility and enough metadata for guest notices such as visitor name, duration, and sleeping arrangement notes.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/rules-coordination-ui.test.ts</automated>
  </verify>
  <done>Rules and household coordination objects share one surface, and all time-based coordination remains visible in the unified calendar.</done>
</task>

</tasks>

<verification>
Run the rules/coordination suite and confirm quiet hours, guest notices, and bookings are represented through `calendar_events` activity types rather than bespoke timeline rendering.
</verification>

<success_criteria>
Households can manage policy and coordination from one coherent rules surface, with explicit acknowledgements and calendar-visible quiet hours, guests, and bookings.
</success_criteria>

<output>
After completion, create `.planning/phases/05-maintenance-house-rules/05-02-SUMMARY.md`
</output>
