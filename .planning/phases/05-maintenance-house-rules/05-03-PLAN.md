---
phase: 05-maintenance-house-rules
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - supabase/functions/commit-repair-receipt/index.ts
  - src/lib/receiptWorkflow.ts
  - src/hooks/useReceipt.ts
  - src/hooks/useMaintenance.ts
  - src/hooks/useInventory.ts
  - src/hooks/useChores.ts
  - src/components/receipt/ReceiptReviewCard.tsx
  - src/app/(app)/finances.tsx
  - src/app/(app)/chores.tsx
  - src/__tests__/maintenance-sync.test.ts
autonomous: true
requirements:
  - MANT-06
  - SYNC-02
  - SYNC-11
  - SYNC-13
  - SYNC-14
must_haves:
  truths:
    - "Resolving a maintenance item with a cost prompts a prefilled expense-split path instead of silently creating an expense."
    - "A repair receipt can link to a maintenance request through the existing review-first receipt architecture."
    - "Cleaning chores surface supply-aware warnings before or after completion using existing inventory thresholds and related-supply data."
  artifacts:
    - path: "supabase/functions/commit-repair-receipt/index.ts"
      provides: "Atomic server-side commit for repair receipt to expense and maintenance linkage."
      contains: "maintenance_request_id"
    - path: "src/hooks/useReceipt.ts"
      provides: "Repair receipt review path that can target maintenance linkage."
      contains: "repair"
    - path: "src/app/(app)/chores.tsx"
      provides: "Chore warning and completion prompt surfaces tied to low cleaning supplies."
      contains: "supply"
  key_links:
    - from: "src/app/(app)/finances.tsx"
      to: "src/hooks/useReceipt.ts"
      via: "repair receipts reuse the existing review-first receipt flow with maintenance linkage metadata"
      pattern: "maintenance_request_id|repair"
    - from: "src/hooks/useMaintenance.ts"
      to: "src/app/(app)/finances.tsx"
      via: "resolved-cost prompt opens a prefilled split-expense path rather than direct write"
      pattern: "prefill|split expense|maintenance"
    - from: "src/app/(app)/chores.tsx"
      to: "src/hooks/useInventory.ts"
      via: "cleaning-related chores warn when linked supplies are already low or likely consumed"
      pattern: "low-stock|threshold|inventory"
---

<objective>
Finish Phase 05’s cross-feature coordination: repair receipt sync, maintenance-cost expense prefills, and chore-to-supply warnings.

Purpose: This plan turns maintenance and rules from isolated surfaces into real household synergies across finances, receipts, chores, and inventory.
Output: One repair receipt commit path, one maintenance-to-expense prompt path, and visible cleaning-supply warnings at chore time.
</objective>

<execution_context>
@/Users/jackeytsui/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jackeytsui/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-maintenance-house-rules/05-CONTEXT.md
@.planning/phases/05-maintenance-house-rules/05-RESEARCH.md
@.planning/phases/04-shopping-meals-supplies/04-07-SUMMARY.md
@src/hooks/useReceipt.ts
@src/components/receipt/ReceiptReviewCard.tsx
@src/app/(app)/finances.tsx
@src/hooks/useChores.ts
@src/hooks/useInventory.ts

<interfaces>
From src/hooks/useReceipt.ts:
```typescript
export function useReceipt();
// Extend existing receipt review flow; do not create a second scanner stack.
```

From src/hooks/useMaintenance.ts:
```typescript
export function useMaintenance();
// Must expose a maintenance-cost handoff suitable for a prefilled expense split prompt.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repair receipt linkage and maintenance-cost expense prefills</name>
  <files>supabase/functions/commit-repair-receipt/index.ts, src/lib/receiptWorkflow.ts, src/hooks/useReceipt.ts, src/hooks/useMaintenance.ts, src/components/receipt/ReceiptReviewCard.tsx, src/app/(app)/finances.tsx, src/__tests__/maintenance-sync.test.ts</files>
  <behavior>
    - Test 1: scanning a repair receipt exposes maintenance-link review controls before save.
    - Test 2: confirming a repair receipt links the resulting expense to the selected maintenance request through one coordinated save path.
    - Test 3: resolving maintenance with a cost opens or prepares a prefilled expense split review instead of auto-writing an expense silently.
  </behavior>
  <action>Expand the maintenance sync test scaffold first. Reuse the existing receipt pipeline from finances and the atomic grocery commit pattern from Phase 04 to implement a repair-specific commit function that links the expense to a maintenance request. Extend the receipt review UI only as much as needed to select or confirm the maintenance target. In parallel, add a maintenance-cost handoff path so resolved requests can launch a prefilled split-expense review flow. Keep the user in control of the final expense save.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/maintenance-sync.test.ts</automated>
  </verify>
  <done>Repair costs and repair receipts connect maintenance to finances through the existing review-first expense architecture.</done>
</task>

<task type="auto">
  <name>Task 2: Add cleaning-supply warnings and completion prompts to chores</name>
  <files>src/hooks/useInventory.ts, src/hooks/useChores.ts, src/app/(app)/chores.tsx, src/__tests__/maintenance-sync.test.ts</files>
  <action>Implement the two SYNC chore-supply behaviors using existing inventory thresholds and chore metadata: show low-supply warnings on relevant chores before work starts, and prompt on completion when a cleaning chore plausibly consumed supplies. Scope the behavior to cleaning-related chores or chore-area mappings only; do not show generic prompts on every chore. Reuse the existing inventory alert state where possible so warnings stay consistent with the supplies screen.</action>
  <verify>
    <automated>npm test -- --runInBand src/__tests__/maintenance-sync.test.ts</automated>
  </verify>
  <done>Chores now reflect real household supply state, making Phase 05 coordination visible before the dashboard and notification layers arrive in Phase 06.</done>
</task>

</tasks>

<verification>
Run the focused maintenance-sync suite and confirm repair receipts still use the existing receipt review architecture while chore warnings are driven by inventory state rather than hardcoded copy.
</verification>

<success_criteria>
Phase 05 closes with real cross-feature coordination: maintenance can turn into an expense review, repair receipts stay atomic and review-first, and cleaning chores become supply-aware.
</success_criteria>

<output>
After completion, create `.planning/phases/05-maintenance-house-rules/05-03-SUMMARY.md`
</output>
